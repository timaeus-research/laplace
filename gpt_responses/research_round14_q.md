# Consult: germbij analytic layer, fourteenth research round (after research_round13)

Same setting (Lean 4 + Mathlib, laplace `Laplace.Multi`, mirrored on the local hironaka branch `wall-atlas`). Every item of your round-13 ranking is now landed, sorry-free, standard axioms:

1. **General-unit boundary theorem** (`ActiveTruthDegenerateGeneral.lean`, `tendsto_modelKernel_general_degenerate`), exactly your statement: partial traces `∀ z u ∈ (0,ρ), Tendsto (fun y ↦ W (Function.update y (Sum.inr 1) z) u) (𝓝[box] 0) (𝓝 (Wtr z u))` (same for `a`), jointly measurable traces with the bounds on `(0,ρ)²`, and the limit `A ρ^{∑(r+1)} |det M|⁻¹ (∫⁻ vWeightTD).toReal vol(F')` with
   `vWeightTD v = 1_{h>h₀} 1_{(M⁻¹v)_1>0} ofReal(Wtr(z(v),u(v)) e^{-(βs+ηh)} e^{-c_ρ atr(z(v),u(v)) e^{-s}})`, `z(v) = ρ e^{-(M⁻¹v)_1}`, `u(v) = truthOf h`, `c_ρ = B ρ^{∑κ}`. Proof: the weighted fibre limit with the second hyperplane dropped and the fibre path `update (negExpMap (update lift (inr 1) L)) (inr 1) z(v)`, the same domination.
2. **Exact push-forward bridge** (hironaka `TruthPushforward.lean`, `exists_truth_pushforward`): `∃ ε > 0, ∃ D : TruthChartsData m T (L ∩ {|T| ≤ ε}), ∀ θ measurable, Measurable (D.totalKernel θ) ∧ ∀ η measurable, ∫⁻_{L ∩ {|T| ≤ ε}} θ(z) η(T z) = ∫⁻ η(s) D.totalKernel θ s`. And the **density certificate** (`exists_truthChartsData_certified`): in addition, `∃ (kF, a)` with `a i` continuous nonvanishing and `F (D.rep i u) = a i u ∏ u^(kF i)` on the boxes, and `∃ (hJ, bJ, wt)` with `bJ i` continuous nonvanishing, `wt i` continuous in `[0,1]`, and `D.dens i u = wt i u * |bJ i u * ∏ u^(hJ i)|` on the boxes (the Jacobian unit and the partition weight, the fields of the Euclidean phase record).
3. **Chart wrapper of the boundary regime** (`ActiveTruthChartDegenerate.lean`): `activeTruthDegMeasure` = the density `C (wt|b|)(P v) e^{-βs-ηh} e^{-c₀ |a(P v)| e^{-s}}` on `degSet = {h>h₀} ∩ {(M⁻¹v)_1>0}` pushed forward along `v ↦ rep(P v)`, `P v = bridgePt(survPt(z(v))) u(v)` (surviving coordinate at `e(inr 1)`, other active coordinates at the wall), `C = A ρ^{∑(r+1)} vol(F')/|det M|`; `tendsto_modelKernelOf_activeTruth_degenerate`, `TermData.activeTruthDegenerate`. The `j = 0` mirror is absorbed by the splitting `e`.
4. **Positivity** (`ActiveTruthDegeneratePositive.lean`): `activeTruthDegMeasure_pos_of_open` (open `O ∋ rep(P v₀)` with `v₀ ∈ degSet`, `wt|b|(P v₀) > 0`, positive face polytope ⇒ `0 < μ O`), `_ne_zero`, `_eq_zero_of_disjoint`.
5. **Certificate-level dominant term** (`ActiveTruthLeadingTerm.lean`): `ofTermData_leadingMeasure_eq_of_lt`: in the certificate assembled from term data of any shapes, a term `p` with `(T p).lam < (T q).lam` for all `q ≠ p` has `leadingMeasure = (T p).μ`; `leadingMeasure_eq_of_unique`; `leadingMeasure_eq_sum_filter`.

Previously landed and still relevant: support-separated distinguishability (`exists_observable_of_activeTruth_separated`), compact-σ uniformity, the weighted-mixed trichotomy, the general-truth export (`exists_truthChartsData`), the mixed record `mixData` (identity chart of the square, `T = xy`, logarithmic fibre kernel) with `mix_tendsto_totalKernel`, `mix_tendsto_fibre_expectation`.

Open research items that were parked earlier in the programme (from the log-face work): the exact log-face constant (the constant of the logarithmic endpoint `PowerLogAssembly`/`LogSandwich` is sandwiched, not exact), and "LP-uniqueness ⇔ profile integrability" (a conjectured equivalence between uniqueness of the LP optimum and integrability of the profile of the leading term).

## Questions

**Q1 (closure audit).** With items 1–5, do you agree the Euclidean layer of the note's active-truth and mixed-truth claims is closed by your round-13 criterion ("exact transport supported by the export; asymptotic consumers have certified data")? If not, name the missing statement precisely.

**Q2 (the certified export as a consumer interface).** The certified export gives, per chart, `(kF, a)`, `(hJ, bJ, wt)`, `(S, q, k)` and the transport. The Euclidean phase record `WallChartsData.Phase` additionally carries two-sided bounds `ma ≤ |a| ≤ Ma`, `mb ≤ |b| ≤ Mb` on the closed boxes (available from compactness) and the exponent bookkeeping (`pExp`, `phaseExp`, `kappa`, `rExp`, `nu`). Is a `TruthChartsData.Phase` record on the laplace side (general truth, with the same fields) the right next object, so that the term theorems (`TermData.vertex/tied/partial/activeTruth/activeTruthDegenerate`) apply to the resolution of `F · T` for a NONLINEAR `T`, or is there a reason to keep the general-truth analysis at the level of `mixData`-style hand instances? Concretely: which of the term theorems used `ℓ` (the coordinate truth) beyond the `truth` field — I believe the model kernel `modelKernelOf` is built from `bridgePt` (which inserts the solved coordinate at `k i` — this is where the coordinate structure of the truth entered) — so a general-truth Phase would need the `bridgePt`/`solvedCoord` construction re-done for `T ∘ rep = S ∏ u^q` (same monomial form!), i.e. it should go through unchanged. Do you see an obstruction?

**Q3 (the mixed instance through the export).** Given Q2, the natural end-to-end test is: the resolution of `F · T` with `T = z₀ z₁` (or any `T` with a non-degenerate but nonlinear zero set), the certified export, a general-truth Phase, and the mixed-truth conclusion (`mix_tendsto_totalKernel`-type: the logarithmic coefficient at the corner) reproduced from the exported charts rather than from the identity chart. Is that the right integration test, and what is its minimal form (which term theorem fires on which chart)?

**Q4 (the two parked research items).** For the exact log-face constant and the LP-uniqueness ⇔ profile-integrability equivalence: state each precisely as you understand it from the note, say whether you believe it is true, and whether either is worth a bounded formalisation round now or should remain parked.

**Q5 (ranking).** Rank the remaining work (Q2's Phase record, Q3's instance, Q4's items, anything you add), with a one-line rationale each.
