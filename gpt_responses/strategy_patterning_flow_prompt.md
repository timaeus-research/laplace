Strategic consult before formalising. Context: Lean 4 + Mathlib (v4.33.0 pin) project `laplace` (attached: CLAUDE.md gotchas, and the existing Sampler track files ULA.lean and Lyapunov.lean). I want to formalise the routine claims of an attached working note (main.tex, "Patterning flow") as small, clearly stated theorems, aks-style (zero sorry at commit), then tag the TeX with \leanref. Please assess the plan: for each target say (a) is the statement as I intend it correct and adequately hypothesised, (b) the lightest Lean/Mathlib route (name the Mathlib lemmas if you know them; flag any you are unsure exist), (c) an estimated retained-line count, and (d) anything I should drop or restate. Do not write full proofs; skeletons and idioms are what I need.

Targets (new directory Laplace/Patterning/):

T1 Horizon.lean (note Prop 3.1). For a matrix H : Matrix ι ι ℝ, η ε : ℝ, b : ι → ℝ, define the recursion δ_{k+1} = (1 - η H) δ_k - (ε η) b, δ_0 = 0. Claim: δ_T = -(ε η) (Σ_{k<T} (1 - ηH)^k) b (matrix geometric sum, by induction). Eigen-form: if H.mulVec v = λ • v then (Σ_{k<T} (1-ηH)^k).mulVec v = ((1 - (1-ηλ)^T)/λ) • v for λ ≠ 0, and = (T:ℝ) • v for λ = 0 (real geom_sum_eq). Continuous-time version skipped.

T2 Profile.lean (note eq. (profile), Prop 13.1, Prop 3.2 second part). Using the repo's ar1_var_iterate (s_{k+1} = ρ² s_k + v from 0 gives v/(1-ρ²)(1-ρ^{2k})): with ρ = 1 - εq/2 and v = ε, the per-mode variance after k steps is (1 - (1 - εq/2)^{2k}) / (q (1 - εq/4)) for 0 < εq < 4. Define the Gaussian profile λ̂(k) := 1/2 Σ_i nβ λ_i · var_i(k) with q_i = nβ λ_i + γ. Claims: (i) first step λ̂(1) = 1/2 nβ ε Σ λ_i (initial slope = sharpness); (ii) plateau (k → ∞) = 1/2 Σ nβ λ_i / (q_i (1 - ε q_i/4)) (Tendsto, using |1 - εq/2| < 1); (iii) the matrix form: for P := nβ • H + γ • 1 symmetric positive definite and the repo's ulaCov P h (stationary covariance (P - (h/2)P²)⁻¹, eigenbasis entries 1/(p_i(1 - h p_i/2))), nβ · 1/2 · trace(H * ulaCov P h) = 1/2 Σ_i (p_i - γ)/(p_i (1 - h p_i/2)) where p_i are P's eigenvalues (route: H = (P - γ•1)/nβ, so trace(H X) = (trace(P X) - γ trace X)/nβ, and use the eigenbasis entries via the conjugation lemma ulaCov_conj_apply). (iv) the continuous-time count 1/2 Σ λ_i/(λ_i + ρ) with ρ = γ/nβ as the h → 0 value: (p_i - γ)/p_i = λ_i/(λ_i+ρ). Question: is there a slicker way to get trace(H * ulaCov) in the eigenbasis than going through P - γ•1?

T3 Direct.lean (note Prop 4.1, Cor 4.2). Matrices A : Matrix ι κ ℝ (d×k), G : Matrix ι ν ℝ (d×n), R : Matrix ι ι ℝ symmetric; χ := -(1/n) Aᵀ R G (k×n); C := (1/n) G Gᵀ; S := Aᵀ R C R A (k×k), assume IsUnit S.det. ω := -Gᵀ R A S⁻¹ dμ. Claims: χ.mulVec ω = dμ; χ χᵀ = (1/n) S; force f := (1/n) G.mulVec ω = -(C R A S⁻¹).mulVec dμ; minimal-norm: for any ω' with χ ω' = dμ, ‖ω‖² ≤ ‖ω'‖² (ω ∈ range χᵀ, ω' - ω ∈ ker χ, orthogonality in dotProduct). Cor 4.2 algebra: if C = H, R = (H + ρ•1)⁻¹ with H + ρ•1 invertible, then S = Aᵀ R A - ρ Aᵀ R R A and Aᵀ (R A S⁻¹ dμ) = dμ + ρ Aᵀ R R A S⁻¹ dμ. Question: cleanest way to do the minimal-norm argument with Matrix.dotProduct / mulVec / vecMul in Mathlib?

T4 SGDLyapunov.lean (note Prop 13.2). (i) For any Σ with H Σ + Σ H = c • C (continuous Lyapunov), trace(H Σ) = (c/2) trace C (take traces; trace(ΣH) = trace(HΣ)). (ii) If C = k • H then Σ = (c k / 2) • 1 solves it, and 1/2 trace(H Σ) = (c k/4) trace H. (iii) Discrete recursion via the repo's diagLyapunov / covStep_diagonal_fixed_iff: for A = diagonal (1 - η λ_i), N = (η²/B) c • diagonal λ_i, the fixed point is diagonal with entries η c /(B (2 - η λ_i)) (requires |1 - ηλ_i| < 1), and 1/2 Σ λ_i σ_i = (η c/(2B)) Σ λ_i/(2 - η λ_i).

T5 Virial.lean (note Prop 13.3, and the ULA correction). (i) Gaussian identity: for the quadratic U = 1/2 xᵀ Q x with Q symmetric positive definite and X a covariance with trace(Q X) known: this is just algebra, so instead state: for the ULA law X = ulaCov P h of P (the repo's), Σ_i p_i x_ii = Σ_i 1/(1 - h p_i/2) and this equals d + (h/2) Σ_i p_i/(1 - h p_i/2), i.e. E[x·∇U] = d + (h/2) E‖∇U‖² with E‖∇U‖² = trace(P² X) (note the repo uses step h with noise 2h, i.e. h = ε/2 in the note's convention, so ε/4 becomes h/2). (ii) The genuine integration-by-parts virial identity in ONE dimension: for U : ℝ → ℝ differentiable with ∫ exp(-U) finite and appropriate decay, ∫ x U'(x) exp(-U x) dx = ∫ exp(-U x) dx. Question: which Mathlib lemma gives integration by parts on the whole real line with limits at ±∞ (I recall integral_mul_deriv_eq_deriv_mul with Tendsto hypotheses at atTop/atBot in Mathlib.Analysis.SpecialFunctions.Integrals or MeasureTheory.Integral.IntegrationByParts)? What hypotheses make this cleanest (e.g. x exp(-U x) → 0 at ±∞ plus integrability of the two integrands)? Is the d-dimensional version realistically formalisable (divergence theorem is not in Mathlib) or should I stop at 1D plus the Gaussian d-dimensional case?

T6 FourGon.lean (note Prop 12.1). Population TMS losses on W : Matrix (Fin 2) (Fin 5) ℝ: ℓ_i(W) = (1/3)[(1 - ‖W_i‖²)² + Σ_{j≠i} relu(W_j·W_i)²] with relu z = max 0 z; L_h = Σ h_i ℓ_i. Claims: (a) the trig-relu identity relu(cos θ)² + relu(sin θ)² + relu(-cos θ)² + relu(-sin θ)² = 1 (since relu(z)² + relu(-z)² = z²); (b) along W_4 = r(cos θ, sin θ) with the alive features at the 4-gon, L_h - L_h(4-gon) = (1/3)[Σ_{j<4} h_j relu(cos(θ-θ_j))² - h_4] r² + (h_4/3) r⁴, and at uniform h this is r⁴/15; (c) volume of {x ∈ ℝ² : ‖x‖⁴/15 < ε} = π √(15 ε) (Mathlib: EuclideanSpace.volume_ball / InnerProductSpace.volume_ball for Fin 2; is the constant available as π r² in a usable form?). I will skip the gradient-vanishing claim (would need fderiv of relu²) and the descending path unless you see a cheap route.

T7 Positivity.lean (note Prop 6.1, the sublevel-set core). For K K_ω : α → ℝ on a measurable set U with c₁ K ≤ K_ω ≤ c₂ K on U, c₁ > 0: the sublevel sets satisfy {K_ω < ε} ∩ U ⊆ {K < ε/c₁} ∩ U and ⊇ {K < ε/c₂} ∩ U, hence μ({K_ω<ε}∩U) is sandwiched (measure_mono). The RLCT-exponent conclusion is left informal.

T8 (maybe) Jacobi: d/ds log det(H + s M)|_{s=0} = trace(H⁻¹ M) for H invertible — does Mathlib have the derivative of det (HasDerivAt / fderiv of Matrix.det), e.g. Matrix.det differentiability via ContinuousMultilinearMap? If not cheaply available, I drop T8.

Also: is there anything in Mathlib for Isserlis / Gaussian fourth moments in finite dimension (T9, note Prop 8.1) that would make E[(δᵀAδ)(δᵀBδ)] = tr A tr B + 2 tr(AB) for δ ~ N(0, I) a short proof? If not, I drop it.

Please end with (1) a recommended attack order, (2) the targets you would drop, (3) any statement you think is false or under-hypothesised.
