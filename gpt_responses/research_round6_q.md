# Consult: germbij analytic layer, sixth research round (after research_round5)

Same setting (Lean 4 + Mathlib, laplace `Laplace.Multi`, mirrored on hironaka `wall-atlas`). Everything you ranked in round 5 except the two research-level items is landed; this round asks for an audit and for the exact statement of the remaining research item.

## Landed since round 5 (all zero-sorry, standard axioms)

1. **Parameter stability, moving form** (`ParameterStability.lean`, `WallLogTermParam.lean`, `PartialTiedParam.lean`). The fully tied constant-unit kernel with `A t → A₀`, `B t → B₀ > 0`, cutoff `D t · t^{-γ/q} → 0` (`tendsto_tiedConst_param`); the variable-unit tied kernel with the same moving constants (`tendsto_modelKernel_tied_param` — the LogExactConstant sandwich with the constants threaded through pointwise in `t`, generated by substitution from the fixed proof); chart level along `σ(t) → σ₀ ≠ 0` (`tendsto_modelKernelOf_tied_param`, `tendsto_termKernel_tied_param`; admissibility of a sign branch is eventually constant, `admissible_eventually_iff`); the assembly along `σ(t)` (`tendsto_fibre_expectation_lex_param`). For the partially tied constant-unit kernel I used a different route: the kernel is linear in `A`, independent of `D` past the cutoff, and **antitone in the scale `B`**; an abstract squeeze (`tendsto_of_antitone_param`: a family antitone in a parameter with fixed-parameter limits `L b` continuous at `B₀` converges along any `B t → B₀`) lifts the fixed theorem `tendsto_modelKernel_partial` to moving constants with no new analysis (`tendsto_modelKernel_partial_param`). NOT done: the locally uniform `sup_{σ∈C}` form (I have only the moving-parameter form; the sup form follows by a compactness/subsequence argument I have not written), and the variable-unit partial-face moving version (`W(x, v_t)`, `a(x, v_t)` see `D`; would need the substitution route through the 436-line variable-unit proof).

2. **The certificate and its constructor** (`TermMeasureCertificate.lean`, `TermData.lean`). `TermMeasureCertificate P σ γ` = per term `(lam, kk, μ)` with `μ` finite, the leading order `(lam₀, k₀)`, and `tendsto` against every continuous nonnegative bounded observable **supported in `L'`** (I first wrote the class without the support condition and corrected it: the term theorems only certify supported observables, because the domain indicator at the wall point is invisible only for them). `leadingMeasure = ∑_{(lam_p,kk_p)=(lam₀,k₀)} μ_p`; `tendsto_fibre_expectation` (ratio → `∫ψ dμ_*/∫χ dμ_*`), `normalise_eq_of_forall_tendsto` (two certified phases with the same leading expectations on an open `L'` have the same normalised leading measure). `TermData P σ γ p` is the per-term input, produced by `ofNotAdmissible` (zero measure), `vertex` (`(termLam, 0, termMeasure)`), `tied` (`(γp+δλ, k, ofReal c • dirac (rep 0))` with `c = tiedConst A B δ κ r λ ρ |a(0)| (wt(0)|b(0)|)`), `partial` (`(γp+δλ, k, partialMeasure)`); `TermMeasureCertificate.ofTermData` assembles, computing the leading order (`inf'` of the powers, `sup` of the log orders on the minimal power).

3. **Atlas independence a.e.** (`KernelAtlasIndependence.lean`): `TruthChartsData.totalKernel_ae_eq`, `WallChartsData.totalKernel_ae_eq`.

4. **Weighted mixed truth, `h > 0`** (`MixedTruthWeighted.lean`): `tendsto_weightedMixed` — `∫_{σ/(bt)}^{b} x^{h−1} f(x, σ/(tx)) dx → ∫_0^b x^{h−1} f(x,0) dx` for `f` continuous on `ℝ²` (dominated convergence, bound `C x^{h−1}`).

5. Wording narrowed as you asked (a.e., growing log mass for bounded observables). The hironaka contract for a general truth was recorded (resolve `F · T` in place of `F · z_ℓ`; the atlas is already the simultaneous monomialisation of the two factors of the product; threading `ℓ ↦ T` through `WallChart/WallAtlas/WallExport/WallIntegral/Theorem*` is mechanical) but not carried out.

## Statements (Lean, verbatim)

```lean
/-- **The constant-unit tied kernel with moving constants.** -/
theorem tendsto_tiedConst_param {ρ γ p q δ : ℝ} {κ r : Fin (k + 1) → ℝ} {w₀ a₀ : ℝ}
    {A B D : ℝ → ℝ} {A₀ B₀ : ℝ} (hρ : 0 < ρ) (hκ : ∀ i, 0 < κ i) {lam : ℝ} (hlam : 0 < lam)
    (htied : ∀ i, (r i + 1) / κ i = lam) (hA : Tendsto A atTop (𝓝 A₀))
    (hB : Tendsto B atTop (𝓝 B₀)) (hB₀ : 0 < B₀) (ha₀ : 0 < a₀) (hδ : 0 < δ)
    (hDcut : Tendsto (fun t ↦ D t * t ^ (-(γ / q))) atTop (𝓝 0)) :
    Tendsto (fun t ↦ t ^ (γ * p + δ * lam) / log t ^ k *
        modelKernel ρ (A t) (B t) (D t) γ p q δ (0 : Fin (k + 1) → ℝ) κ r (fun _ _ ↦ w₀)
          (fun _ _ ↦ a₀) t)
      atTop (𝓝 (tiedConst A₀ B₀ δ κ r lam ρ a₀ w₀)) := by
  obtain ⟨c, hcdef⟩ : ∃ c : ℝ → ℝ, c = fun t ↦ B t * a₀ * ρ ^ (∑ i, κ i) := ⟨_, rfl⟩

with `A t → A₀`, `B t → B₀ > 0` and the cutoff `D t · t^{-γ/q} → 0`. -/
theorem tendsto_modelKernel_tied_param {ρ γ p q δ : ℝ} {κ r : Fin (k + 1) → ℝ}
    {W a : (Fin (k + 1) → ℝ) → ℝ → ℝ} {A B D : ℝ → ℝ} {A₀ B₀ : ℝ} (hρ : 0 < ρ)
    (hκ : ∀ i, 0 < κ i) {lam : ℝ} (hlam : 0 < lam) (htied : ∀ i, (r i + 1) / κ i = lam)
    (hA : Tendsto A atTop (𝓝 A₀)) (hA0 : ∀ᶠ t in atTop, 0 ≤ A t) (hB : Tendsto B atTop (𝓝 B₀))
    (hB₀ : 0 < B₀) (hD0 : ∀ᶠ t in atTop, 0 ≤ D t)
    (hDcut : Tendsto (fun t ↦ D t * t ^ (-(γ / q))) atTop (𝓝 0)) (hδ : 0 < δ) {aL wU : ℝ}
    (haL : 0 < aL) (hW : Measurable (Function.uncurry W)) (ha : Measurable (Function.uncurry a))
    (hW0 : ∀ x v, 0 ≤ W x v) (hWup : ∀ x v, W x v ≤ wU)
    (halow : ∀ x v, (∀ j, x j ∈ Ioo (0 : ℝ) ρ) → 0 ≤ v → v < ρ → aL ≤ a x v)
    {w₀ a₀ : ℝ} (ha₀ : 0 < a₀) (hw₀ : 0 ≤ w₀)
    (hWlim : Tendsto (Function.uncurry W) (𝓝 (0, 0)) (𝓝 w₀))
    (halim : Tendsto (Function.uncurry a) (𝓝 (0, 0)) (𝓝 a₀)) :
    Tendsto (fun t ↦ t ^ (γ * p + δ * lam) / log t ^ k *
        modelKernel ρ (A t) (B t) (D t) γ p q δ (0 : Fin (k + 1) → ℝ) κ r W a t) atTop
      (𝓝 (tiedConst A₀ B₀ δ κ r lam ρ a₀ w₀)) := by
  have hr : ∀ i, -1 < r i := fun i ↦ by
    have h := htied i
    have hk := hκ i

theorem WallChartsData.Phase.tendsto_fibre_expectation_lex_param {D : WallChartsData m ℓ L'}
    {F : (Fin (m + 1) → ℝ) → ℝ} (P : D.Phase F) {σ : ℝ → ℝ} {σ₀ γ : ℝ}
    (hS : ∀ i, |D.S i| = 1) (hF : ∀ z, 0 ≤ F z) (hFm : Measurable F)
    (hσ : Tendsto σ atTop (𝓝 σ₀)) (hσ₀ : σ₀ ≠ 0) {ψ χ : (Fin (m + 1) → ℝ) → ℝ}
    (hψc : Continuous ψ) (hψ : ∀ z, 0 ≤ ψ z) {Mψ : ℝ} (hMψ : ∀ z, ψ z ≤ Mψ) (hχc : Continuous χ)
    (hχ : ∀ z, 0 ≤ χ z) {Mχ : ℝ} (hMχ : ∀ z, χ z ≤ Mχ)
    {lam : WallChartsData.Phase.TermIdx D → ℝ} {kk : WallChartsData.Phase.TermIdx D → ℕ}
    {Cψ Cχ : WallChartsData.Phase.TermIdx D → ℝ} {lam₀ : ℝ} {k₀ : ℕ}
    (hmin : ∀ p, lam₀ ≤ lam p ∧ (lam p = lam₀ → kk p ≤ k₀))
    (hKψ : ∀ p, Tendsto (fun t ↦ t ^ lam p / log t ^ kk p * P.termKernel ψ (σ t) γ p t) atTop
      (𝓝 (Cψ p)))
    (hKχ : ∀ p, Tendsto (fun t ↦ t ^ lam p / log t ^ kk p * P.termKernel χ (σ t) γ p t) atTop
      (𝓝 (Cχ p)))
    (hpos : (∑ p ∈ Finset.univ.filter (fun p ↦ lam p = lam₀ ∧ kk p = k₀), Cχ p) ≠ 0) :
    Tendsto (fun t ↦
        (D.totalKernel (fun z ↦ ENNReal.ofReal (exp (-(t * F z)) * ψ z)) (σ t * t ^ (-γ))).toReal /
        (D.totalKernel (fun z ↦ ENNReal.ofReal (exp (-(t * F z)) * χ z)) (σ t * t ^ (-γ))).toReal)
      atTop
      (𝓝 ((∑ p ∈ Finset.univ.filter (fun p ↦ lam p = lam₀ ∧ kk p = k₀), Cψ p) /
        ∑ p ∈ Finset.univ.filter (fun p ↦ lam p = lam₀ ∧ kk p = k₀), Cχ p)) := by
  classical
  refine (tendsto_sum_ratio_lex hmin hKχ hKψ hpos).congr' ?_
  filter_upwards [eventually_gt_atTop 0, hσ.eventually_ne hσ₀] with t ht hσt

/-! ### The abstract squeeze -/

/-- A family `f b t`, antitone in the parameter `b`, whose normalised limits `L b` exist for every
fixed `b > 0` and are continuous at `B₀`, converges along any `B t → B₀ > 0`. -/
theorem tendsto_of_antitone_param {f : ℝ → ℝ → ℝ} {N L B : ℝ → ℝ} {B₀ : ℝ} (hB₀ : 0 < B₀)
    (hB : Tendsto B atTop (𝓝 B₀)) (hN : ∀ᶠ t in atTop, 0 ≤ N t)
    (hanti : ∀ᶠ t in atTop, ∀ b b', 0 < b → b ≤ b' → f b' t ≤ f b t)
    (hlim : ∀ b, 0 < b → Tendsto (fun t ↦ N t * f b t) atTop (𝓝 (L b)))
    (hL : ContinuousAt L B₀) :

theorem tendsto_modelKernel_partial_param {ρ γ p q δ : ℝ} {κ r : Fin (k + 1) ⊕ ν → ℝ}
    {w₀ a₀ : ℝ} {A B D : ℝ → ℝ} {A₀ B₀ : ℝ} (hρ : 0 < ρ) (hκ : ∀ i, 0 < κ i) {lam : ℝ}
    (hlam : 0 < lam) (htied : ∀ i, (r (Sum.inl i) + 1) / κ (Sum.inl i) = lam)
    (hgap : ∀ j, lam * κ (Sum.inr j) < r (Sum.inr j) + 1) (hA : Tendsto A atTop (𝓝 A₀))
    (hB : Tendsto B atTop (𝓝 B₀)) (hB₀ : 0 < B₀) (ha₀ : 0 < a₀) (hw₀ : 0 ≤ w₀) (hδ : 0 < δ)
    (hDcut : Tendsto (fun t ↦ D t * t ^ (-(γ / q))) atTop (𝓝 0)) (hγq : 0 < γ / q) :
    Tendsto (fun t ↦ t ^ (γ * p + δ * lam) / log t ^ k *
        modelKernel ρ (A t) (B t) (D t) γ p q δ (0 : Fin (k + 1) ⊕ ν → ℝ) κ r (fun _ _ ↦ w₀)
          (fun _ _ ↦ a₀) t)
      atTop
      (𝓝 (A₀ * w₀ * ((B₀ * a₀) ^ (-lam) * δ ^ k *
        (Gamma lam / k.factorial * ∏ i, 1 / κ (Sum.inl i)) *
        ∫ z in Set.pi univ (fun _ : ν ↦ Ioo (0 : ℝ) ρ),
          ∏ j, z j ^ (r (Sum.inr j) - lam * κ (Sum.inr j))))) := by
  have hr : ∀ i, -1 < r i := by
    rintro (i | j)

structure TermMeasureCertificate (P : D.Phase F) (σ γ : ℝ) where
  /-- The power of each term. -/
  lam : TermIdx D → ℝ
  /-- The logarithmic order of each term. -/
  kk : TermIdx D → ℕ
  /-- The coefficient measure of each term. -/
  μ : TermIdx D → Measure (Fin (m + 1) → ℝ)
  finite : ∀ p, IsFiniteMeasure (μ p)
  /-- The leading order: minimal power, then maximal logarithmic order. -/
  lam₀ : ℝ
  k₀ : ℕ
  hmin : ∀ p, lam₀ ≤ lam p ∧ (lam p = lam₀ → kk p ≤ k₀)
  /-- The certified asymptotic of every term against every continuous nonnegative bounded
  observable supported in `L'`. -/
  tendsto : ∀ φ : (Fin (m + 1) → ℝ) → ℝ, Continuous φ → (∀ z, 0 ≤ φ z) → (∃ M, ∀ z, φ z ≤ M) →
    (∀ z, φ z ≠ 0 → z ∈ L') →
    ∀ p, Tendsto (fun t ↦ t ^ lam p / log t ^ kk p * P.termKernel φ σ γ p t) atTop
      (𝓝 (∫ z, φ z ∂(μ p)))

attribute [instance] TermMeasureCertificate.finite

namespace TermMeasureCertificate

theorem tendsto_fibre_expectation (hS : ∀ i, |D.S i| = 1) (hF : ∀ z, 0 ≤ F z) (hFm : Measurable F)
    (hσ : σ ≠ 0) {ψ χ : (Fin (m + 1) → ℝ) → ℝ} (hψc : Continuous ψ) (hψ : ∀ z, 0 ≤ ψ z) {Mψ : ℝ}
    (hMψ : ∀ z, ψ z ≤ Mψ) (hψL : ∀ z, ψ z ≠ 0 → z ∈ L') (hχc : Continuous χ) (hχ : ∀ z, 0 ≤ χ z)
    {Mχ : ℝ} (hMχ : ∀ z, χ z ≤ Mχ) (hχL : ∀ z, χ z ≠ 0 → z ∈ L')
    (hpos : (∫ z, χ z ∂C.leadingMeasure) ≠ 0) :
    Tendsto (fun t ↦ D.fibreRatio F ψ χ σ γ t) atTop
      (𝓝 ((∫ z, ψ z ∂C.leadingMeasure) / ∫ z, χ z ∂C.leadingMeasure)) := by
  unfold leadingMeasure lexMeasure at hpos ⊢

noncomputable def WallChartsData.Phase.TermData.tied (hσ : σ ≠ 0) (hγ : 0 < γ)
    {p : WallChartsData.Phase.TermIdx D} (hadm : D.admissible p.1 p.2.1 p.2.2 σ)
    (hQ : D.Qexp p.1 = 0) (hκ : ∀ j, 0 < P.kappa p.1 j) {lam : ℝ} (hlam : 0 < lam)
    (htied : ∀ j, (P.rExp p.1 j + 1) / P.kappa p.1 j = lam) (hδ : 0 < P.phaseExp p.1 γ) :
    P.TermData σ γ p where
  lam := γ * P.pExp p.1 + P.phaseExp p.1 γ * lam
  kk := k
  μ := ENNReal.ofReal (tiedConst (P.constA p.1 σ) (P.constB p.1 σ) (P.phaseExp p.1 γ) (P.kappa p.1)
    (P.rExp p.1) lam (D.ρ p.1) |P.a p.1 0| (P.wt p.1 0 * |P.b p.1 0|)) • Measure.dirac (D.rep p.1 0)
  finite := ⟨by
    rw [Measure.smul_apply, smul_eq_mul]
    exact ENNReal.mul_lt_top ENNReal.ofReal_lt_top (measure_lt_top _ _)⟩
  tendsto := fun φ hφc hφ ⟨M, hM⟩ hφL ↦ by

theorem tendsto_weightedMixed {b σ h : ℝ} (hb : 0 < b) (hσ : 0 < σ) (hh : 0 < h)
    {f : ℝ → ℝ → ℝ} (hf : Continuous (fun p : ℝ × ℝ ↦ f p.1 p.2)) :
    Tendsto (fun t ↦ ∫ x in Ioo (σ / (b * t)) b, x ^ (h - 1) * f x (σ / (t * x))) atTop
      (𝓝 (∫ x in Ioo 0 b, x ^ (h - 1) * f x 0)) := by
  obtain ⟨C, hC⟩ := (isCompact_Icc.prod isCompact_Icc).exists_bound_of_continuousOn
```

## Questions

1. **Audit** the statements above (signs, constants, hypotheses, the observable class, the tied point mass `c · δ_{rep 0}`, the squeeze lemma's hypotheses, the weighted limit). Is `tendsto_of_antitone_param` correctly stated (antitone only required eventually and for `b > 0`; continuity of `L` at `B₀` only)? Anything over-claimed in "classify every term ⇒ certificate ⇒ ratio limit"?

2. **The degenerate face (round-5 item 4).** Give ONE exact example to formalise, with: `Q ≠ 0` (an active truth constraint) and an active loss constraint, independent active constraints, a positive-dimensional optimal face, an explicit integral asymptotic (the model integral, its normalisation `t^{λ}/(log t)^k`, the limit constant or measure), and the face dimension equal to the log exponent. State it in the model-kernel conventions (`∫_{box} W ∏x^r e^{-B t^δ a ∏x^κ}` on the domain `D t^{-γ/q} ∏ x^{-Q/q} < ρ`), or in raw coordinates if that is cleaner, and say which of the existing theorems (`tendsto_modelKernel_tied`, `tendsto_modelKernel_partial`, `integrable_tiedDom_twoScaled`) it reduces to after a substitution. Is the answer already implied by the two-scaled tied-truth certificate (`TwoScaledInner`, `TiedTruthCertificate`), i.e. is the "degenerate" case with `Δ ≠ 0` and two scaled coordinates actually a zero-dimensional face (`k = 0`, no log), so that the first genuinely degenerate example needs THREE coordinates in the optimal face's affine hull?

3. **Uniformity.** Is the locally uniform `sup_{σ ∈ C}` form needed for the note, or is the moving-parameter form the honest statement (it implies continuous convergence; the sup form follows by the subsequence argument)? If needed, is there a cheaper route than sequences (e.g. monotonicity in `σ` of the kernels through `A, B, D`, as for the partial faces)?

4. **The variable-unit partial faces along a moving parameter**: worth the 436-line substitution, or is there a squeeze (the variable-unit kernel is sandwiched by constant-unit kernels at radii `ρ'` — is the sandwich itself monotone in `B`)?

5. **Ranking** of what remains (general-truth hironaka export; the degenerate example; the sup form; the variable-unit partial moving version; anything new you see now that the certificate closes the assembly, e.g. a statement about what the leading expectations determine — the normalised leading measure — as the note's "what expectation values know" theorem in this setting).
