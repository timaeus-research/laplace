# germbij formalisation — research consult, round 17

Same setting as round 16 (laplace seabed, Lean 4.33 + Mathlib; resolution mirror on the local hironaka branch `wall-atlas`). Round 16's ranking has been worked through; here is what happened, then the questions.

## Outcomes of round 16

1. **Boundary regression repaired** (your statement-level catch was right: the support hypothesis killed the trace). Now `mix_tendsto_totalKernel_boundary_pow`: for the phase `a(z) z₁^n` (`a` continuous, positive on the closed square), observable `z₀^q z₁^p ψ` with `ψ ≥ 0` continuous and NO support hypothesis (the restriction to the square is internal to the record), along `s = σ/τ`, `t = τ^n`:
   `τ^p K_{τ^n}(σ/τ) → σ^q ∫_{2σ}^∞ u^{p−q−1} e^{-u^n a(σ/u,0)} ψ(σ/u,0) du`,
   and the acceptance value `mix_tendsto_totalKernel_boundary_const`: `n = 1`, constant `a`, `ψ ≡ 1`, `p = q+1`: limit `σ^q e^{-2aσ}/a`. The exported version was withdrawn (an observable supported in the region has zero trace; pointwise identification of two chart kernels at `s = σ/t` for an observable not supported in the region is not available — only the a.e. identity from the push-forward).

2. **Boundary `TermData` landed as the zero scale of the existing dominant-scale theorem** (`ZeroScaleCertificate.lean`): in the chart that solves `z₁`, the phase `a z₁` has effective exponent `κ = k − Q·p/q < 0` on the unsolved coordinate and `phaseExp = 1 − γp = 0` at `γ = 1`; `tendsto_modelKernel` applies at `α = 0` unchanged (limiting domain = whole box, tied phase, profile `B a₀(u) ∏u^κ`, unrescaled face = the wall segment). Missing was only the profile certificate: `integrable_box_rpow_mul_exp_neg_prod` (`1_{(0,ρ)^k} ∏u^r e^{-c∏u^κ}` integrable when every `κ_j < 0` or (`κ_j = 0` and `r_j > −1`); bound `e^{-cP} ≤ N!/(cP)^N`), `ProfileIntegrableOf.of_zeroScale`, `TermData.zeroScale` (power `γp`, no log, measure `termMeasure … 0`). The coordinatewise condition is only sufficient (`κ = (−1,1)`: `u₁^{r₁}u₂^{r₂}e^{-cu₂/u₁}` integrable iff `r₁+r₂ > −2`).

3. **The certificate acceptance test was analysed and deferred.** Three API facts: (i) in the identity chart `mixData` the solved coordinate is `z₀` (`k = 0`), so `a z₁` is a phase on the UNSOLVED coordinate: `κ = 1`, `δ = 1`, scale `α = 1` with tied truth (`Q·α = γ = 1`) and tied phase — a single-scaled vertex with tied cut, covered by no landed constructor (`of_vertex` needs strict truth, `of_twoScaled` two scaled coordinates); it is the zero-scale regime only in the chart solving `z₁`. (ii) The term theorems assume `F ≥ 0` globally (`branchReal_le` bounds `e^{-tF} ≤ 1` at every chart point), and a phase record `F ∘ rep = a(u) ∏ u^{kF}` with `kF = (0,1)` and a continuous nonvanishing unit forces `F` to change sign: the linear phase is not admissible as `Phase` data; `a z₁²` at `γ = 1/2` is. (iii) The term theorems take observables supported in `L'`; for `mixData` (`L' = (0,1/2]²`) they vanish on the segment, so the certified leading constant of `z₀^q z₁^p ψ` is `0` for every `p ≥ 1` — the `t^{-p}` decay is a subleading order invisible to the leading-term certificate; only `p = 0` observables on a record whose region contains the segment in its interior (the exported thin region, two branches) see the boundary value. Honest test: exported record over the full thin region, `F = a z₁²`, observable `z₀^q ψ`, a tied-cut single-scaled certificate, `tendsto_nhds_unique` against the by-hand constant (~400 lines).

4. **Certificates from the LP** (`CertificateFromLP.lean`): `UniqueLPMin.comp_equiv`, `ProfileIntegrableOf.of_uniqueLPMin_vertex / _twoScaled`, `TermData.vertexOfUniqueLPMin / twoScaledOfUniqueLPMin`.

5. **Singular flat witness landed** (`FlatInvisibleSingular.lean`): `singular_flat_perturbation_invisible` (`c x^{2k} ≤ L₁` near `0`, `L₁ ≥ c₁` away, `f` flat, `0 ≤ f ≤ M` ⇒ `O(t^{-N})` for all `N`), `_superpolynomial`, `singular_flat_witness_superpolynomial k`: `x^{2k}` vs `x^{2k} + e^{-1/x²}`.

6. Items 2 (1D constructive recovery: `jet_recovery`, `polynomialJet_recovery`, `smooth_full_jet_recovery`, `base_recovery`, `nondegenerateJet_recovery`, …) and 4 (`PowerLogDominance.powLog`: positivity, dominance) were already in the seabed.

## Questions

1. **API weakening.** The two API limitations found in (3) — global `F ≥ 0`, and observables supported in `L'` — look like the real obstruction to acceptance tests and to Astra's "pointwise chart-independence". Which weakening is right and cheapest: (a) `F ≥ 0` only on `L'` (or on the chart images `rep_i(dom_i)`), threading `hF : ∀ z ∈ L', 0 ≤ F z` through `branchReal_le`/`modelG_nonneg`/`totalKernel_toReal`; (b) observables continuous on a neighbourhood of `L'` without a support condition, with the restriction to `L'` internal to the kernel (the term theorems' `hφL` is used through `weightFn`, the `dom`-indicator; where exactly is continuity of `φ` off `L'` needed — the moving-weight limit `tendsto_weightFn`?); (c) both. Is there a cleaner reformulation (e.g. observables as functions on `L'` extended by `0`, with the limiting measure supported on the closure of the wall region) that makes chart-independence of the fibre evaluation at `s = σ t^{-γ}` a theorem rather than an a.e. statement?

2. **Pointwise chart-independence.** Give the sharpest statement you would prove: for two chart systems `D₁, D₂` over regions `L₁, L₂` both containing a neighbourhood of the wall point, and a continuous bounded `θ` (no support condition), when does `D₁.totalKernel θ s = D₂.totalKernel θ s` hold for all `s ≠ 0` small? Our tools: `totalKernel_ae_eq_of_support` (a.e., needs support in `L₁ ∩ L₂`), `continuousAt_totalKernel` (needs support in `L'`), `totalKernel_mul_comp_truth`, the truth cutoff. Is the right route "continuity of `s ↦ ∫⁻_{{T = s} ∩ L'} …` for closed regions with the fibres meeting `∂L'` transversally", or an intrinsic characterisation of the kernel as a conditional density with a continuous version?

3. **Ranking** of: (a) the API weakening (1); (b) the acceptance test as specified in (3); (c) the tied-cut single-scaled certificate (one scaled coordinate, tied truth `Q·α = γ`, tied phase; the limiting domain is `{u > (ρ/D)^{-q/Q}}`, profile `u^r e^{-c u^κ}` integrable for every `r`); (d) LP optimal-vertex existence + classification; (e) the general recession-cone integrability criterion at `α = 0` (mixed-sign `κ`); (f) anything the note's "What remains" needs that we have not listed. Concrete Lean-level statement for the top item and the cheapest first step.
