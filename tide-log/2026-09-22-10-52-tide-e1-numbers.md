# Tide: e1-numbers

**Direction (user):** Just proceed on auto. You can bite off big chunks here to formalise and don't worry about retrospectives.
(Auto-mode continuation: E1's and E5's quoted numbers as exact corollaries of the ULA law.)
**Seabed:** laplace, main 6f2b20a
**Started:** 2026-09-22T10:53Z

## Candidates v1 (Claude)

- A (E1, first finding): the stiffest direction is inflated by exactly `1/(1 − lr·pmax/2)`: `2`, `4`, `20` at `lr·pmax = 1, 1.5, 1.9`
  (`ulaCov_conj_apply` on the diagonal, divided by the Laplace value `1/pᵢ`).
- B (E1, second finding): for `κ = 1` (all `pᵢ` equal) and `d = 10`, `t⟨K⟩ = d/(2(1 − lr·pmax/2)) = 10, 20, 100` at
  `lr·pmax = 1, 1.5, 1.9` (`ula_llc` with constant eigenvalues), against the true `5`.
- C (E5, second finding): Rosenbrock `d = 2`, `lr·pmax = 0.5`, `κ ≥ 2500`: the ULA-corrected LLC lies within `10⁻⁴` of
  `(1 + 4/3)/2 = 7/6 = 1.1667` ("1.17 … the ULA law applied to the stiff direction only").
- D (E1, third finding): `τ_flat = 2/(lr·pmin) = 2κ/(lr·pmax) = 2 × 10⁵` at `κ = 1000`, `lr·pmax = 0.01`.

D was already in the seabed (`iat_flat_e1 : 2/(h pmin) − 1 = 199 999`), so the tide is A+B+C.

## Numerical check

Hand arithmetic: `1/(1 − 1/2) = 2`, `1/(1 − 0.75) = 4`, `1/(1 − 0.95) = 20`; `10/(2·½) = 10`, `10/(2·¼) = 20`, `10/(2·0.05) = 100`;
`½(4/3 + 1/(1 − 1/10000)) − 7/6 = 5.0 × 10⁻⁵`.

## GPT-6 Astra v1

Verbatim in `gpt_e1_numbers_v1.md`. Summary: readings right; the two-eigenvalue value is exactly `7/6 + 1/(2(4κ − 1))`, at most
`1/19998 < 10⁻⁴` above `7/6` for `κ ≥ 2500`; "applied to the stiff direction only" = keep its inflation, approximate the flat factor
by `1`; this certifies the arithmetic of the local Gaussian explanation, not the non-Gaussian Rosenbrock SGLD result, and `1.17` is
rounded, not exactly `(1 + 4/3)/2`. Worth landing as a small coherent tide provided the numbers are connected to `ulaCov_conj_apply`
and `ula_llc` (they are), with C named as a Gaussian proxy (renamed `ula_llc_stiff_flat_bound`). The suggested extra bound
`d/2 ≤ LLC_ULA ≤ d/(2(1 − hpmax/2))` is already `sum_one_div_one_sub_bounds`; skip the log-spaced-spectrum decimals.

## Vote
- Claude: A+B+C
- GPT-6 Astra: A+B+C ("with C explicitly scoped to the Gaussian-proxy arithmetic")

## Correction before landing

The full build failed with "environment already contains `Laplace.Sampler.ula_llc_isotropic` from `Laplace.Sampler.StepSizeTradeoff`":
A's values and all of B were already formalised in tide `stepsize-tradeoff` (`ula_inflation_e1`, `ula_llc_isotropic`, `ula_llc_e1`),
which the survey missed. The tide is trimmed to what is new: the ratio lemma `ulaCov_conj_diag_ratio` (the ULA eigenbasis variance over
the Laplace value equals `1/(1 − hpᵢ/2)`) and C (`ula_llc_two`, `ula_llc_stiff_flat_bound`).

## Result

Commit (see below) on `tide/e1-numbers`; `lake build` clean, `scripts/sorries` 0/0/0/0. **Built locally only**: the seabed was archived
into the resolution monorepo during this run (see the `covK-derivative` log), so the tide is exported as patches in the SRI
(`staging/pending-patches/tide-62-e1-numbers/`). C landed together with the E1 ratio lemma; A's values and B were already in the seabed
(`ula_inflation_e1`, `ula_llc_isotropic`, `ula_llc_e1`, tide `stepsize-tradeoff`) and D as `iat_flat_e1`.
`Laplace/Sampler/E1Numbers.lean` (81 lines): `ulaCov_conj_diag_ratio`, `ula_llc_two`, `ula_llc_stiff_flat_bound`.

Surprises: the first full build failed with "environment already contains `Laplace.Sampler.ula_llc_isotropic`": the survey had missed
that E1's numbers were formalised in `StepSizeTradeoff` (grep the seabed for a proposed name before writing).
