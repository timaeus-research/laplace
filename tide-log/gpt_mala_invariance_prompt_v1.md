You are consulted as GPT-6 Astra in a Lean 4 / Mathlib formalisation loop (the "Tide"). The attached tide log describes the seabed (a Lean repo `laplace`, Mathlib pinned Sep 2026) and the candidate A0–A4 (+ optional B) for one excursion: Metropolis-Hastings with Langevin proposals on a Gaussian target, on `ι → ℝ` with Lebesgue measure, unnormalised densities.

Questions:
1. Are A0–A4 correct as stated? In particular check the general ratio identity A1 (`π(y)q(y,x)/(π(x)q(x,y)) = exp(yᵀGy − xᵀGx)`, `G = S − AᵀSA − P/2`, needing only `Sᵀ = S` and `(SA)ᵀ = SA`), and the specialisations `G = −(h/4)P²` (MALA: `A = 1 − hP`, `S = 1/(4h)`) and `G = −(h/4)P` (pMALA: `A = (1−h)1`, `S = P/(4h)`). Is the MH invariance proof in A0 complete with `q`'s normalisation constant independent of `x` (translation invariance) and no integrability assumptions when everything is a `lintegral` of nonnegative functions?
2. Lean formulation. I plan: `targetWeight P x := exp(−(1/2) x ⬝ᵥ P *ᵥ x)`, `propWeight A S x y := exp(−(y − A *ᵥ x) ⬝ᵥ S *ᵥ (y − A *ᵥ x))`, `accept x y := min 1 (target y * prop y x / (target x * prop x y))`, and the invariance statement as
   `∫⁻ x, ofReal (π x) * ((∫⁻ y in A, ofReal (q x y * α x y)) / Z + (1 − (∫⁻ y, ofReal (q x y * α x y)) / Z) * A.indicator 1 x) = ∫⁻ x in A, ofReal (π x)`
   with `Z := ∫⁻ y, ofReal (q x y)` (a constant). Is this the right shape, or should I use `ProbabilityTheory.Kernel` (`Kernel.withDensity`, `Measure.bind`)? Any pitfalls with ENNReal subtraction `1 − a x` (need `a x ≤ 1`, which follows from `α ≤ 1` and the definition of `Z`) and with division by `Z`?
3. Tonelli: for the first term I need `∫⁻ x, ofReal(π x) * ∫⁻ y in A, ofReal(q x y α x y) = ∫⁻ y in A, ∫⁻ x, …` — `lintegral_lintegral_swap` needs `AEMeasurable (uncurry f) (μ.prod ν)`; the functions are continuous in `(x, y)` except for `min`/division — is `Measurable` by `fun_prop`/`Measurable.min`/`Measurable.div` routine here? Should I avoid the division by writing `α` as `min (π x q x y) (π y q y x) / (π x q x y)`, or better, avoid `α` altogether in the invariance proof by rewriting `π(x)q(x,y)α(x,y) = min(π(x)q(x,y), π(y)q(y,x))` first (symmetric in x,y)?
4. Does the September 2026 Mathlib give the density of `multivariateGaussian μ S` (for `S` positive definite) with respect to Lebesgue on `EuclideanSpace`? If not, is the density-level statement A4 the right endpoint for this tide?
5. Sizing and anything better close to this seabed (e.g. also stating that ULA's ratio is *not* 1, i.e. the acceptance is `< 1` for some proposals, or the expected acceptance rate)? Vote for a candidate set at the end (`Vote: A`, `Vote: A+B`, …) with wording qualifications.
