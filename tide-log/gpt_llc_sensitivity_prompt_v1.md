You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD LLC estimation. The attached tide log states the seabed and candidates A–D with a numerical check. Please answer tersely:

1. Mathematics. Are A–D correct? Check (i) the weighted Chebyshev identity `∑ᵢⱼ vᵢvⱼ(rᵢ − rⱼ)(fᵢ − fⱼ) = 2[(∑ v r f)(∑ v) − (∑ v r)(∑ v f)]` and that `Monovary f r` (in Mathlib: `∀ i j, r i < r j → f i ≤ f j`) makes each term nonnegative; (ii) that the two instances have weight ratio `uᵢ/vᵢ = pᵢ` exactly (LLC weights `1/(1 − h pᵢ/2)` vs trace weights `1/(pᵢ(1 − h pᵢ/2))` in E8; `1` vs `1/pᵢ` in E1), so C and D are literally A with `rᵢ = pᵢ`; (iii) whether "the LLC is the sensitive diagnostic" is fairly captured by the *trace* (the note's E5 line uses the Frobenius norm — is there an analogous statement for the relative Frobenius error `‖Σ_ULA − P⁻¹‖_F/‖P⁻¹‖_F` vs the relative LLC inflation, e.g. via the same inequality applied to `f²` with weights `1/p²`, or is Frobenius genuinely different?); (iv) the `Monovary c̃ p` reading of "the noise lands on the stiff directions", and whether one should instead assume the weaker `∑ᵢⱼ (1/(pᵢ(1−hpᵢ/2)))(1/(pⱼ(1−hpⱼ/2)))(pᵢ − pⱼ)(c̃ᵢ − c̃ⱼ) ≥ 0` (a covariance-type condition) which is what the proof actually uses.

2. Lean. Is there a weighted Chebyshev / rearrangement lemma in Mathlib (`MonovaryOn.sum_smul_sum_le_card_smul_sum` is uniform-weight), or is the direct double-sum expansion (`Finset.sum_mul_sum`, `← Finset.mul_sum`, `← Finset.sum_mul`, then `ring`) the way? For the termwise nonnegativity with `rcases lt_trichotomy (r i) (r j)`, is `mul_nonneg_of_nonpos_of_nonpos`/`mul_nonneg` + the monovary hypothesis the right idiom, or `Monovary`'s own API (`Monovary.symm`, `monovary_iff_forall_smul_nonneg`? there is `monovary_iff_smul_rearrangement`-type material in `Mathlib/Order/Monotone/Monovary.lean`)? For `Uᵀ P⁻¹ U = diag(1/pᵢ)` the seabed exhibits inverses (`Matrix.inv_eq_left_inv` with `diagonal_mul_diagonal`); fine to repeat?

3. Scope. A+B+C+D one tide (~250 lines)? Cheap valuable additions: the Laplace-limit instance (uniform LLC weights vs `1/pᵢ` trace weights for any comonotone `f`, i.e. C with general `f`), or the "reverse under antitone noise" remark as an actual theorem (`Antivary c̃ p → tr inflation ≥ LLC inflation`)?

End with a single line `Vote: <A+B+C|A+B+C+D|A+B+C+D+extras|other>` and one sentence of justification.
