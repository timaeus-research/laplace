You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD (ULA) LLC estimation. The attached tide log states the seabed and four candidate statements A–D with a Monte Carlo check. Please answer tersely:

1. Mathematics. Are A–D correct as stated? Check in particular: (i) `E LLĈ = Λ_h − shortfall` with `shortfall = ½ ∑ᵢ (R₂ᵢ/N)/(1 − h pᵢ/2)`, `R₂ᵢ = ∑_{k<N} ρᵢ^{2(b+1+k)}` (this is the seabed's `integral_llc_running_mean`: `t E[…] = ½ ∑ⱼ (1 − R₂ⱼ/N)/(1 − h pⱼ/2)`); (ii) the sign bookkeeping in C, `E LLĈ − d/2 = inflation − shortfall`; (iii) the RMS composition `√(a + b²) ≤ √a + |b|` and the resulting three-term bound in D; (iv) whether the hypothesis `0 < h pᵢ ≤ 1` (needed for the variance envelope, `ρᵢ ∈ [0,1)`) is the right common hypothesis, or whether B–D should be stated under `h pᵢ < 2` with the envelope hypothesis added only where needed.

2. Which target is the right *user-facing* one for the note's sentence "within 2% of d/2 for every budget above 10⁴ draws, the residual being the ULA inflation": C (MSE against d/2), D (RMS with three named terms), or B (MSE against the ULA LLC, which isolates the Monte Carlo + burn-in part)? Should the RMS bound also be given relative to `d/2`, i.e. divided by `d/2`, and if so in what form (per-direction sums, or the spectrum-free `(h pmax/2)/(1 − h pmax/2)` for the inflation and `ρ_max^{2(b+1)}/(N(1 − ρ_max²)(1 − h pmin/2))`-type bound for the burn-in)?

3. Lean. For A, is there a Mathlib lemma giving `∫ (X − a)² = Var + (E X − a)²` in explicit-integral form, or should it be proven directly (expand the square, `integral_add/sub`, `integral_const`) as the seabed did for the summed version `integral_sum_sq_sub_eq`? For `√(a + b²) ≤ √a + |b|`: the cleanest route (`Real.sqrt_le_sqrt` + `Real.sqrt_le_left`? `Real.sqrt_le_iff`? `Real.le_sqrt`?) — name the lemma class, not exact identifiers if unsure. Any pitfall in composing the seabed's mean theorem (hypothesis `∀ c, iIndepFun (ξ c) P`, per chain) with the variance theorem (hypothesis `iIndepFun (fun a : Fin C × ℕ => ξ a.1 a.2) P`, joint)? (The joint one implies the per-chain one by `iIndepFun.precomp` with an injection; is that the right bridge?)

4. Scope: A+B+C+D one coherent tide (~250 lines)? Anything cheap and valuable nearby (e.g. the isotropic explicit RMS form, or the E4 remark "one chain of 1000 draws gives 0.90 at d = 2" as an instance of the shortfall)?

End with a single line `Vote: <A+B|A+B+C|A+B+C+D|other>` and one sentence of justification.
