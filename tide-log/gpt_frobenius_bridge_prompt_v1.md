You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD LLC estimation. The attached tide log states the seabed and candidates A–D with a numerical check. Please answer tersely:

1. Mathematics. Are A–D correct? In particular (i) `tr((UᵀAU)ᵀ(UᵀAU)) = tr(AᵀA)` needs both `UᵀU = 1` and `UUᵀ = 1` (or just one for square matrices?); (ii) C: with `⟨uᵢ, x⟩ = ∑ₐ Uₐᵢ xₐ`, `Σ̂_eig,ᵢⱼ = (1/(CN))∑_{c,k} ⟨uᵢ,x⟩⟨uⱼ,x⟩ = (Uᵀ Σ̂_raw U)ᵢⱼ` pointwise — fine; and the *centred* Frobenius sums agree because `E Σ̂_eig = Uᵀ (E Σ̂_raw) U` by linearity and `Σ̂_eig − EΣ̂_eig = Uᵀ(Σ̂_raw − EΣ̂_raw)U`; (iii) D as a restatement of `frobenius_ula_le` — any subtlety in the integrability of the raw entries (the plan derives it from `Σ̂_raw = U Σ̂_eig Uᵀ` and the known `L⁴` of the eigen-projections, avoiding a separate Gaussian argument for the coordinate functions)?

2. Lean. `EuclideanSpace.inner_eq_star_dotProduct` + `star_trivial` + the seabed's `orthoCol hQ i = toLp 2 fun j => orthoOf hQ j i` to get `⟨orthoCol i, v⟩ = ∑ₐ U a i * v a`; then `Finset.sum_mul_sum`, `Finset.mul_sum`, `Finset.sum_comm` for the double-sum rearrangement of `(∑ₐ Uₐᵢxₐ)(∑ᵦ Uᵦⱼxᵦ)`; `Matrix.mul_apply` twice for `(UᵀMU)ᵢⱼ = ∑ₐ∑ᵦ Uₐᵢ Mₐᵦ Uᵦⱼ` — any cleaner idiom (e.g. `Matrix.mul_apply'`, `dotProduct`, `Matrix.vecMul`/`mulVec` forms)? For the integral of a matrix-valued expression entrywise: `integral_finsetSum` + `integral_const_mul` — fine? Is there any advantage to phrasing D via Mathlib's `Matrix.frobenius_norm` (scoped instance) instead of `∑ᵢⱼ Aᵢⱼ²`?

3. Scope. A+B+C+D one tide (~250 lines)? Cheap valuable additions: `E Σ̂_raw` in closed form (`U diag(s₂ᵢ(1 − R₂ᵢ/N)) Uᵀ`, from `integral_pooledSecondMoment`), or the target-norm form of `frobenius_pooledSecondMoment_target` (error against `Σ_ULA` in raw coordinates)?

End with a single line `Vote: <A+B|A+B+C|A+B+C+D|A+B+C+D+extras|other>` and one sentence of justification.
