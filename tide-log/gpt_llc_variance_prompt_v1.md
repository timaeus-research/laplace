You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD (ULA) LLC estimation. The attached tide log states the seabed (what is already formalised) and four candidate statements A–D with a Monte Carlo check. Please answer, concretely and tersely:

1. Mathematics. Are A, B, C, D correct as stated? In particular check: (i) the cross-covariance factor `(δ_{ii'}δ_{jj'} + δ_{ij'}δ_{ji'})` in A from the four-way Wick identity `Cov(XY, ZW) = E[XZ]E[YW] + E[XW]E[YZ]` and the `(c,i)`-diagonal Gram table; (ii) the exact LLC variance `1/(2CN²) Σ_i p_i² Σ_{k,l} H_i(k,l)²` when the statistic is `t·(1/(CN))ΣΣ ½⟨x,Hx⟩` with `P = tH`, eigenvalues `p_i` of `tH`, using `½⟨x,Hx⟩ = Σ_i p_i/(2t) ⟨u_i,x⟩²`; (iii) the envelope constant `Σ_{k,l<N} (ρ²)^{|k−l|} ≤ N(1+ρ²)/(1−ρ²)`; (iv) the isotropic ratio `2/(d(d+1))` in D.

2. Lean architecture. The seabed already has `variance_pooledSecondMoment` (the case i'=i, j'=j of A) proven from `cov_mul_mul_of_isLinComb` by expanding the double sums and simplifying the `if c = c' ∧ i = j` deltas. Should A be proven as a fresh general theorem (and `variance_pooledSecondMoment` left as is), or should the existing theorem be re-derived from A? For B, is it better to state the statistic as `Σ_i w i * pooledSecondMoment x N b i i ω` and use bilinearity `Var(Σ w_i Y_i) = Σ_{i,j} w_i w_j Cov(Y_i, Y_j)` (does Mathlib have a clean finite-sum covariance-bilinearity lemma for explicit-integral covariances, or should we prove `variance_sum_eq_sum_cov`-style directly, as the seabed already did for the Frobenius law), then kill the off-diagonal terms with A?

3. For C, the seabed's route in `frobenius_ula_le` was: `fourthMomentTable_projNoise_dir` (Gaussian table for `projNoise h (orthoCol i) (ξ c) k` over `Fin C × ℕ × ι`), `inner_ulaChain_eq_realChain`, `isLinComb_realChain_dir`, `gram_realChain_dir`, then the abstract theorem. Any pitfalls in reusing exactly this route for the LLC statistic, in particular in rewriting `½⟨x,Hx⟩` into `Σ_i p_i/(2t) ⟨u_i,x⟩²` inside the pooled sum and the integral, and in matching `s₂ i = 2h/(1−ρ_i²)` to `1/(p_i(1−h p_i/2))` (`ula_variance_eq` exists: `2h/(1-(1-hp)^2) = 1/(p(1-hp/2))` under `0<h, 0<p, hp<2`)?

4. Scope. Is A+B+C (+D) one coherent tide (~400 lines)? Anything cheap and valuable nearby that we are missing (e.g. an exact-vs-envelope corollary, the `κ`-dependent version of D, or the "one chain of 1000 draws gives 0.90 at d = 2" finite-chain mean shortfall already covered by `llc_running_mean_shortfall`)?

End with a single line `Vote: <A|A+B|A+B+C|A+B+C+D|other>` and one sentence of justification.
