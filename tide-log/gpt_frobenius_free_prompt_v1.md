You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD LLC estimation. The attached tide log states the seabed and candidates A–C with a numerical check. Please answer tersely:

1. Mathematics. Are A–C correct? Check (i) `∑ᵢⱼ (1 + δᵢⱼ) aᵢ aⱼ τᵢⱼ ≤ τmax[(∑a)² + ∑a²] ≤ τmax (d+1) ∑a²` for `a ≥ 0`, `0 ≤ τᵢⱼ ≤ τmax` (is `τᵢⱼ ≥ 0` needed, or only `aᵢaⱼ ≥ 0` and `τᵢⱼ ≤ τmax`?); (ii) `(1 + ρᵢρⱼ)/(1 − ρᵢρⱼ) ≤ (1 + r²)/(1 − r²)` from `0 ≤ ρᵢ ≤ r < 1`; (iii) the reading of C against E4's "√(d/(CN)) with a prefactor set by the autocorrelation times" — the prefactor here is `√((d+1)/d · τ(r²))`, the autocorrelation time of the *squared* flattest coordinate (≈ `10κ` at `h pmax = 0.1`); is that the right gloss, and is the bound tight in the isotropic case (yes by `llc_vs_frobenius_isotropic`'s computation) and loose by up to a factor `d` when the spectrum is spread (the numerical check shows 4–6 at `κ = 100`)? (iv) Is `∑ᵢ s₂ᵢ² = ‖Σ_ULA‖_F²` immediate (orthogonal invariance of the Frobenius norm) — the tide states the denominator as `∑ s₂ᵢ²` and will *say* it is the Frobenius norm; is that acceptable or should the identity be proved?

2. Lean. `sq_sum_le_card_mul_sum_sq : (∑ i ∈ s, f i)² ≤ #s * ∑ i ∈ s, f i²` is in Mathlib. For the per-term bound `(if i = j then 2 else 1) * (aᵢaⱼτᵢⱼ) ≤ τmax(aᵢaⱼ) + τmax·[i = j] aᵢaⱼ`, `split_ifs` + `nlinarith` — fine? For collecting the double sum into `τmax (∑a)² + τmax ∑a²`: `Finset.sum_add_distrib`, `Finset.mul_sum`, `Finset.sum_mul_sum` (for `(∑a)² = ∑∑ aᵢaⱼ`), `Finset.sum_ite_eq` — the standard idiom? Any pitfall with the `(if i = j then 2 else 1) / (C * N) * (…)` shape of the seabed's `frobenius_ula_le` (division inside the summand)?

3. Scope. A+B+C one tide (~150 lines)? Cheap valuable additions: the RMS form `√(E‖·‖²)/‖Σ_ULA‖_F ≤ √((d+1)τ(r²)/(CN))`, or the `κ`-form at `h pmax = 1/10` (`τ(r²)` with `r = 1 − 1/(10κ)`)?

End with a single line `Vote: <A+B|A+B+C|A+B+C+extras|other>` and one sentence of justification.
