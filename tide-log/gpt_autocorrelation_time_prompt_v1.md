You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD (ULA) LLC estimation. The attached tide log states the seabed and candidates A–E with a numerical check. Please answer tersely:

1. Mathematics. Are A–E correct as stated? Check (i) the closed form of the weighted geometric sum `∑_{m<N} (N − m − 1) ρ^{m+1} = ρ(N(1−ρ) − (1−ρ^N))/(1−ρ)²` and hence A; (ii) the running-mean variance B from the Gram table `s₂(ρ^{|k−l|} − ρ^{k+l})` at the window times `b+1+k` (is `∑_{k,l} ρ^{(b+1+k)+(b+1+l)} = (∑_k ρ^{b+1+k})²` the right factorisation, and does `|(b+1+k) − (b+1+l)| = |k − l|` make the Toeplitz part shift-invariant?); (iii) the limit C and the claim that the transient term is `O(1/N)`; (iv) D's exact identity `(1+ρ)/(1−ρ) = 2/(hp) − 1` for `ρ = 1 − hp`; (v) whether "integrated autocorrelation time" is the right name for `lim N Var(x̄_N)/s₂` for the *zero-start* chain (the standard definition uses the stationary chain; the two limits agree — should the log say so explicitly?).

2. Lean. For A: induction on `N` with `Finset.sum_range_succ` and `geom_sum_eq` (the summand contains `N`, so the inductive step needs the summand rewritten as `(N − m − 1) ρ^{m+1} + ρ^{m+1}`), or is there a Mathlib lemma for `∑ (N − i) x^i`-type sums (`Finset.sum_range_id_mul_two`? `geom_sum_deriv`-style)? For B: products of two `L²` functions are integrable — is the lemma `MemLp.integrable_mul` (statement shape `Integrable (fun x => f x * g x)` or `Integrable (f * g)`)? For C: the idiom for `c/N → 0` over `ℕ` (`tendsto_const_div_atTop_nhds_zero_nat`) and `ρ^N → 0` (`tendsto_pow_atTop_nhds_zero_of_lt_one`), combined with `Filter.Tendsto.mul/sub/const_mul`; any pitfall in the ℕ-to-ℝ casts (`Nat.cast_add_one_pos`, `(N : ℝ) ≠ 0` from `N ≠ 0`)? Should the limit be stated along `atTop` on `ℕ` with the statistic defined for all `N` (and `N = 0` giving `0/0 = 0`), or restricted to `N ≥ 1`?

3. Scope. Is A+B+C+D+E one coherent tide (~300 lines)? Anything cheap and valuable nearby, e.g. the *stationary* AR(1) variance of the sample mean in closed form (`s₂/N · [(1+ρ)/(1−ρ) − 2ρ(1−ρ^N)/(N(1−ρ)²)]`), the effective sample size `N/τ`, or the E1 sentence "at κ = 1000 and lr pmax = 0.01 the flattest direction has τ_flat = 2×10⁵ steps" (an instance of D)?

End with a single line `Vote: <A+B+C|A+B+C+D|A+B+C+D+E|other>` and one sentence of justification.
