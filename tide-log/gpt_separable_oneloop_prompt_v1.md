You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the mathematics of a note on Hessian-vs-sampling sanity checks for SGLD LLC estimation. The attached tide log states the seabed and candidates A–D with a numerical check. Please answer tersely:

1. Mathematics. Are A–C correct? In particular (i) the bubble `(TSST)ᵢⱼ = ∑ Tᵢₖₗ Sₖₘ Sₗₙ Tⱼₘₙ` and the line tadpole `(T·S·(T:S))ᵢⱼ = ∑ₖₗ Tᵢⱼₖ Sₖₗ (T:S)ₗ` both reduce to `αᵢ² sᵢ² δᵢⱼ` for the separable tensor on a diagonal `S`; (ii) hence `Πᵢᵢ = −(t/2) gᵢ sᵢ + t² αᵢ² sᵢ²` and with `sᵢ = 1/(tλᵢ)` the entry `1/(λᵢt) + (αᵢ²/λᵢ⁴ − gᵢ/(2λᵢ³))/t²`, matching the seabed's 1D value; (iii) C's relative correction `(a² − 1/2)/t` under `αᵢ² = a²λᵢ³`, `gᵢ = λᵢ²` — and is the sign right (negative for `a² < 1/2`, so the Laplace variance *over*predicts for small anharmonicity)? (iv) Is stating B/C in the eigenframe (H diagonal) a faithful formalisation of the note's rotated potential `u = Qᵀ(w − w*)`, given the functional's covariance transforms as `Q · Qᵀ` under the rotation of `H, T, Q` (D)?

2. Lean. The seabed defines `contractQ`, `contractT`, `bubble`, `tadpoleLine`, `oneLoopPi`, `oneLoopCov` on `Fin d` with `Matrix.of` and nested `∑`. For the separable tensors `sepT α i j k = if i = j ∧ j = k then α i else 0`, is the cleanest evaluation route repeated `Finset.sum_eq_single i` (killing each index in turn, `simp [hk]` for the off-index terms) followed by `by_cases hij : i = j`, or a `simp` with `Finset.sum_ite_eq`-style lemmas after rewriting the products of `if`s into a single `if` on a conjunction? For `(t • diagonal λ)⁻¹ = diagonal (1/(tλ))`: `Matrix.inv_eq_right_inv` with `diagonal_mul_diagonal`, or is there `Matrix.inv_diagonal`/`Matrix.smul_inv` in the pinned Mathlib?

3. Scope. A+B+C as one tide (~250 lines)? Is D worth doing now (how heavy is rotating a rank-3 and rank-4 tensor with `Fin d` sums in Lean), or should it stay a follow-up? Anything cheap and valuable nearby, e.g. the E2 sentence "`a² < 3` keeps the minimum unique" (the discriminant `α² < 3λg` of the seabed's 1D theorems reads `a² < 3` in the note's parametrisation), or the per-direction "second-order error `O(t⁻²)` relative" statement (which would need a third-order rate the seabed lacks)?

End with a single line `Vote: <A+B|A+B+C|A+B+C+D|other>` and one sentence of justification.
