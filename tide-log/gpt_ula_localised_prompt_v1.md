You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD (ULA) LLC estimation. The attached tide log states the seabed and candidates A–C with a numerical check. Please answer tersely:

1. Mathematics. Are A–C correct? Check (i) `Uᵀ P² U = (Uᵀ P U)²` needs `U Uᵀ = 1` inserted between the factors; (ii) the ULA law's eigenvalues `1/(aᵢ(1 − h aᵢ/2))` for `P = tH + γI` and the stability condition `h aᵢ < 2` (i.e. `h(tλ_max + γ) < 2`, so localisation *tightens* the step-size constraint); (iii) B's formula and that its `γ = 0` and `h → 0` limits are the seabed's `ula_llc` and `localisedLLC`; (iv) C's two-sided bound, and whether the per-direction excess `(h aᵢ/2)/(1 − h aᵢ/2)` is indeed largest on the stiffest direction so that "the residual is the 5% ULA inflation of the stiff directions" is the right gloss (the measured excess is 1.5%, 2.3%, 4.0% at γ_rel = 1, 10, 100 — increasing with γ because localisation makes all directions stiff relative to pmax: is that reading right?).

2. Lean. The seabed proved `Uᵀ (tH + γI)⁻¹ U = diag(1/(tλ+γ))` by exhibiting the inverse and `Matrix.inv_eq_left_inv`. For `ulaCov P h = (P − (h/2) • (P * P))⁻¹` the same route needs `Uᵀ (P − (h/2)•(P*P)) U = diag(a − (h/2)a²)`: is `Matrix.mul_sub`/`Matrix.sub_mul` + `Matrix.mul_smul`/`smul_mul` + the conjugation lemma for `P` + a `P*P` conjugation (`Uᵀ P P U = Uᵀ P U Uᵀ P U`) the cleanest, or should one instead reuse the seabed's `ulaCov_eq_lyapunov`/`lyapunovVia_conj_apply` machinery (which conjugates by an arbitrary orthogonal `U` with `Uᵀ U = 1`)? For `diagonal d₁ - diagonal d₂`, is the lemma `Matrix.diagonal_sub`? Any pitfall in `(h/2) • (P * P)` vs `(h/2) • P * P` parsing (the seabed's `ulaCov` uses `(h / 2) • (P * P)`)?

3. Scope. A+B+C as one tide (~200 lines)? Cheap valuable additions: the localised ULA stability condition as a theorem (`ula_fixed_iff` exists for general `P`: state it in `H`'s eigenvalues as `h(tλ_max + γ) < 2`), the per-direction ULA-corrected localised variances (E3 left panel) `1/(aᵢ(1 − h aᵢ/2))`, or the E3 caption's "ULA-corrected form" of the *trace* ratio?

End with a single line `Vote: <A+B|A+B+C|A+B+C+extras|other>` and one sentence of justification.
