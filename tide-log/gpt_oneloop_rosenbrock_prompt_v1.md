You are consulted as GPT-6 Astra in a Lean 4 + Mathlib (Sep 2026 pin) formalisation "tide". Read the attached tide log (Candidates v1) and the attached Lean source `Rosenbrock.lean` (exact Rosenbrock moments) and the statement excerpts of the 1D theorems in the log. Answer tersely:

1. Correctness. (a) Are the index conventions in my Lean transcription of the note's `eq:oneloop` (contractQ, bubble = TSST, tadpoleLine = T·S·(T:S), Π, Cov = S + SΠS with S = (tH)⁻¹) the standard one-loop connected two-point function for `e^{-t L}`, `L = ½wᵀHw + (1/6)Tw³ + (1/24)Qw⁴`? Check the signs and the factors t, t². (b) Check the hand computation for Rosenbrock (Π = 2a² (2,−1)(2,−1)ᵀ, SΠS = (2/t²) e_y e_yᵀ). (c) Check the 1D bookkeeping: with `cubicScale λ α = α/(6 λ^{3/2})` and `quarticScale λ γ = γ/(24 λ²)` (please confirm this is the natural normalisation making the rescaled perturbation `A u³/√t + B u⁴/t`; if the seabed defines them differently the algebra changes), `(45A² − 12B)/λ − (α/(2λ²))² = α²/λ⁴ − γ/(2λ³)`.

2. Lean route. (a) For `oneLoopCov_rosenbrock` I plan `rw [rosenCov_eq_laplace_add]`, a lemma `(t • rosenHess a)⁻¹ = (1/t) • !![1,2;2,4+1/a]` via `Matrix.inv_eq_right_inv`, then `ext i j; fin_cases i <;> fin_cases j <;> simp [defs, Fin.sum_univ_two, Matrix.mul_apply] <;> field_simp <;> ring`. The bubble is a 4-fold sum over `Fin 2` inside a triple matrix product: is `simp`+`ring` going to cope (16 terms per entry, then S·Π·S), or should I first prove `oneLoopPi t (rosenT a) (rosenQ a) S = 2a² • !![4,−2;−2,1]` as a separate lemma and only then multiply by S? (b) For the tensors, nested vector notation `![![![12*a, −2*a], ![−2*a, 0]], ![![−2*a, 0], ![0, 0]]] : Fin 2 → Fin 2 → Fin 2 → ℝ` versus `fun i j k => if … then … else 0`: which simp-normalises better under `Fin.sum_univ_two` and `fin_cases`? (c) For the 1D asymptotic theorem: the seabed gives `∃ K T, 0 ≤ K ∧ 1 ≤ T ∧ ∀ {t}, T ≤ t → |t⟨x²⟩ − 1/λ − c₂/(λt)| ≤ K/(t√t)` and (I believe) `Tendsto (fun t => t * ⟨x⟩) atTop (nhds (−α/(2λ²)))` (statement to be confirmed). Give the 5–8 line Mathlib idiom to turn the bound into `Tendsto (fun t => t*(t⟨x²⟩ − 1/λ)) atTop (nhds (c₂/λ))` (squeeze with `K/√t → 0`: `tendsto_of_tendsto_of_tendsto_of_le_of_le'`? `Metric.tendsto_atTop`? `Filter.Tendsto.congr'` with `Real.tendsto_inv_sqrt`?), and then to combine with the mean limit into the variance limit.

3. Is there a stronger nearby statement I should include (e.g. the exactness of the one-loop formula for every "curved valley" `valley μ g a` with quadratic `g`, or for any potential whose Taylor series terminates at order 4 in one direction; or the Rosenbrock d = 2 second-order LLC)? End with a one-line vote: A, B, C, or a modification.
