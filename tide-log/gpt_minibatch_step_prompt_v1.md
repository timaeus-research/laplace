You are consulted as GPT-6 Astra in a Lean 4 + Mathlib (Sep 2026 pin) formalisation "tide". Read the attached tide log (Candidates v1 with numerical check) and the attached Lean sources: Minibatch.lean (finite population correction `fpc_bilinear₂`/`fpc_bilinear`, `batchMean`, `popMean`, `sum_powersetCard_sum`, E8 constants) and FullStep.lean (`e8FullStep`, `fullStep`, `fullLinear`, `stateTerm`, `ulaStep`, `minibatchNoise`, `minibatchCoeff`). Terse answers:

1. Correctness of A.1–A.4, in particular the single-family trick `(H_B − H) w − g_B = mean_B a − ā` with `aᵢ = Hᵢ w − gᵢ` (using `∑ gᵢ = 0`), the sign of the cross terms after expansion, and that averaging the pointwise identity against a centred law with second moment `Σ` gives exactly `e8FullStep (t•H) h t C_g Hs H m Σ` with `C_g = c ∑ gᵢgᵢᵀ` — please check against the attached definitions (`fullLinear`, `minibatchNoise`, is it `A * X * A` or `A * X * Aᵀ`, `2h•1 + h²t²•C`?). Also: the note's `C_g` is the gradient-noise covariance *at `w*`*, `(1/m)(1 − m/n) S²` with `S²` the `n−1` sample covariance of the `gᵢ` — consistent with `c ∑ gᵢgᵢᵀ` when `∑ gᵢ = 0`?

2. Lean route for the Gaussian step: I plan an entrywise definition `E_ξ[F] := Matrix.of fun i j => (∫ ξ, F ξ i j * gaussianWeight (matCLM 1) ξ) / gaussianZ (matCLM 1)` and the seabed's second moments (`integral_coord_mul_gaussianWeight_matCLM` with `P = 1`, `P⁻¹ = 1`) plus vanishing first moments. Then `E_B` is `(n.choose m)⁻¹ • ∑ B`. For the law form (A.4) I plan scalar Bochner integrals against a `[IsProbabilityMeasure μ]` on `ι → ℝ` with hypotheses `∫ w, w i ∂μ = 0`, `∫ w, w i * w j ∂μ = Σ i j`, `Integrable (fun w => w i)`, `Integrable (fun w => w i * w j)`, expanding every matrix entry as a polynomial of degree ≤ 2 in `w` (finite sums, `integral_finsetSum`, `integral_mul_left`, `integral_const` with `measure_univ = 1`). Versus a matrix-valued Bochner integral with `ContinuousLinearMap.integral_comp_comm` for `X ↦ A * X * Aᵀ` under `open scoped Matrix.Norms.Operator`. Which is less painful? Any pitfall with `Matrix.of`/`Matrix.ext` and `simp` on `(A * X * Aᵀ) i j = ∑ k, ∑ l, A i k * X k l * A j l`?

3. Is there a cleaner or stronger nearby statement (e.g. state the one-step map as an affine map on second moments and the stationary equation `Σ = e8FullStep Σ` as a corollary with `fullFixed`; or the mean-variance pair as a joint recursion)? One-line vote: A, B, C, or a modification.
