You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD LLC estimation. The attached tide log states the seabed and candidates A–D with a numerical check. Please answer tersely:

1. Mathematics. Are A–D correct? Check (i) `tr C_g = (1/m)(1 − m/n) tr S²` with `S²` the `n − 1`-normalised sample covariance of the per-sample gradients (sampling without replacement); (ii) B: substituting `h = (h pmax)/pmax`, `pmax = t λmax` into `h t² tr C_g/(2d)` gives `(h pmax) t (1 − m/n) tr S²/(2 d λmax m)` — so the note's rule of thumb drops the finite-population factor `(1 − m/n)` (≈ 1 for `m ≪ n`) and the `1/(1 − h pmax/2)` upper factor; is it right to present the note's expression as the *lower* end of a two-sided bound? (iii) C and D as stated (any sign or domain subtlety with `ε`, `1 − h pmax/2 > 0`, `m ≤ n`); (iv) the "step size shrinking like `1/t²`" reading: with `C_g` fixed (it depends on `w*` and the data, not on `t`) and the constraint `h t² tr C_g ≤ 2dε(1 − h pmax/2)`, `h` must scale as `t⁻²` — but `pmax = t λmax` also grows with `t`, so `h pmax = O(1/t) → 0` and the ULA inflation vanishes; is that the intended gloss?

2. Lean. `sampleCov g = (n−1)⁻¹ • ∑ vecMulVec (gᵢ − ḡ) (gᵢ − ḡ)`: positive semidefiniteness via `Matrix.posSemidef_sum` and `posSemidef_vecMulVec_self_star`-type lemmas (for real vectors `star` is trivial), then `.smul`; is there a pitfall with `Matrix.PosSemidef.smul` requiring `0 ≤ c` in a `StarOrderedRing` form? For the trace, `Matrix.trace_smul` and `Matrix.trace_sum`; anything else? For C and D, pure real inequalities from B by `div_le_iff₀`/`le_div_iff₀` and monotonicity in `m` (`1/m` decreasing) — fine?

3. Scope. A+B+C+D one tide (~150 lines)? Cheap valuable additions: the E8 instance numbers (the note quotes "2% at `t = n/log n`, 13% at `t = n`, 130% at `t = 10n` with `m = 32`" for the `d = 20` logistic model — these need `tr S²/λmax`, which the note does not print, so probably not), or the linear-in-`t` / inverse-in-`m` monotonicity statements as theorems?

End with a single line `Vote: <A+B|A+B+C|A+B+C+D|A+B+C+D+extras|other>` and one sentence of justification.
