You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks (Metropolis–Hastings / MALA). The attached tide log states the seabed and candidates A–D with a numerical check. Please answer tersely:

1. Mathematics. Are A–C correct as stated? In particular: (i) the decomposition A of `∫_A π K(·, B)` into the flux double integral over `A × B` plus the rejection mass on `A ∩ B` (the rejection part of `K(x, ·)` is `(1 − a(x)) δ_x`, so restricted to `B` it is `1_B(x)(1 − a(x))`); (ii) that B follows from Tonelli on `μ|_A ⊗ μ|_B` and `flux(x,y) = flux(y,x)` with no integrability hypothesis (everything is in `[0, ∞]`); (iii) that C (law version) is B divided by the normaliser `T`; (iv) whether "reversibility" is the right name for the rectangle statement (as opposed to the symmetric joint measure, D) — is the rectangle version equivalent to D given that rectangles generate the product σ-algebra and the measures are finite?

2. Lean. The seabed's `mh_invariant` proof: `simp only [mhKernelSet, mul_add]`, `lintegral_add_left` (with measurability of `x ↦ π(x) (∫⁻ y in E, w x y)/Z`), an `e1` rewriting `π(x)·(∫⁻ y in E, w)/Z` as `Z⁻¹ ∫⁻ y in E, flux x y` via `lintegral_const_mul'`, then `lintegral_const_mul' _ _ hZinv`, `lintegral_lintegral_swap hFm.aemeasurable`, `mhFlux_comm`, and `lintegral_indicator` for the Dirac part. For the restricted version: is `lintegral_lintegral_swap` directly applicable with `μ.restrict A` and `μ.restrict B` (product of restricted measures; `SFinite` instances for restrictions), and is `Measure.restrict_restrict hB : (μ.restrict A).restrict B = μ.restrict (B ∩ A)` the right lemma for the Dirac part? For C, `Measure.restrict_withDensity hA` then `lintegral_withDensity_eq_lintegral_mul` (needs measurability of the density and of `x ↦ K(x, B)`, which the seabed has as `measurable_mhKernelSet`) — any pitfall with the `∘` forms (the seabed notes typed `have`s in lambda form are needed)? For D, is there a Mathlib extensionality lemma for measures on `X × X` agreeing on measurable rectangles (finite measures), e.g. `Measure.ext_of_generateFrom_of_iUnion` with `generateFrom_prod`, or `Measure.prod_eq`-style uniqueness?

3. Scope. A+B+C one tide (~200 lines)? Is D worth attempting now (how many lines if the rectangle extensionality exists)? Anything cheap nearby (e.g. self-adjointness of the kernel on `L²(ν)` for bounded measurable `f, g`: `∫ f(x) (Kg)(x) dν = ∫ g(x) (Kf)(x) dν` — this follows from B by linearity for indicator functions and needs a monotone-class/simple-function argument for general `f, g`; probably a follow-up)?

End with a single line `Vote: <A+B|A+B+C|A+B+C+D|other>` and one sentence of justification.
