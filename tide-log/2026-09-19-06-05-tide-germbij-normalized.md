# Tide: germbij normalized singular identifiability

**Direction (user):** "Ok let's continue with the formalisation and exploration of this. Examine the current state of the paper and the Lean, and note that with hironaka and the grey book formalised we might be able to progress this." (germbij, 2026-09-19; the user also authorised notes in the Overleaf file `germbij_slop.tex`, with incorporation into the main text directed by them.)
**Seabed:** laplace, commit 012767e (main, 2026-09-06)
**Started:** 2026-09-19T06:05Z
**Worktree:** `learning-theory/lean/laplace-tide-germbij-normalized`, branch `tide/germbij-normalized`

## Seabed snapshot

Paper: `learning-theory/papers/germbij/germbij.tex` (Overleaf 6a79f2faaebbc50b7a1dd53d, commit 9954504, 2026-08-26, 557 lines). Structure: thm:recovery / cor:analytic (nondegenerate, fully formalised, 21 leanref markers pinned at laplace 9171d40), prop:flat (flat perturbations invisible, formalised), sec:singular, thm:singular (unnormalized pencil theorem, formalised end to end: `pencil_families_force_eq_near_smooth`), q:proportional (the normalized/projective question), prop:one-point (formalised: `one_point_anchoring_contradiction`), constructive classes (1D formalised; quasi-homogeneous partially, coarea step commented out), summary. The global-picture section, Morse–Bott item and SQH induction tail are commented out "pending formalisation" (solid-core commit 2654d05, 2026-08-10).

**New since the Lean work (commit 9954504, 2026-08-26, "arul.shnkr"):** a 33-line `\begin{proof}` was inserted directly under Question q:proportional answering it **No** by integration by parts: from $\int \partial_y\phi\, e^{-tL} = t\int \phi\, \partial_y L\, e^{-tL}$ and the proportionality hypothesis applied to the observables $\partial_y\phi$ and $\phi\,\partial_y L_1$, the unknown scalar $C(t)$ cancels and one gets $\int \phi\, \partial_y(L_1-L_2)\, e^{-tL_2} = o(t^{-\infty})$ for all $\phi$; taking $\phi \mapsto \phi\,\partial_y(L_1-L_2)$ and applying lem:sector forces the germ of $\partial_y(L_1 - L_2)$ at every $p \in W_0$ to vanish, so $L_1 - L_2$ is locally constant, hence $0$. This settles the normalized formulation in full generality with no anchoring point. It is not yet a theorem environment in the note, has no leanref, and is not in Lean. The prose after it (“We expect the answer is no, and a single point … suffices”) still describes the pre-proof state.

Lean (laplace, 171 modules, 53 germbij-related). Reusable for the singular direction:
- `pencil_families_force_germ_eq_at_smooth`, `pencil_families_force_eq_near_smooth` (Laplace/Multi/SingularSmooth): the note's thm:singular with the $C_c^\infty$ premise; assembly point → open neighbourhood of $W_0$.
- `sector_lower_bound_multi` (Multi/Sector), `leading_part_scaled_set` (Multi/LeadingPart), `analytic_remainder_bound` and `analytic_pencil_difference_lower_bound_multi` (Multi/AnalyticBridge), `quadratic_upper_bound_of_nonneg`, `exists_least_nonzero_diagonal`, `exists_bump_one_on_ball` (Multi/SingularPrep), `contDiff_mul_of_tsupport_subset` (Multi/SingularSmooth).
- `SuperPoly` (Anchoring.lean): `∀ N, f =o[atTop] t^{-N}`; `superPoly_of_mul_anchor`, `anchored_proportionality_remove_scalar`, `one_point_anchoring_contradiction` (OnePointAnchoring.lean) — the projective hypothesis is already phrased there as `SuperPoly (I₂ - C t * I₁)` with an arbitrary `C : ℝ → ℝ`.
- Mathlib: `integral_mul_fderiv_eq_neg_fderiv_mul_of_integrable` (Analysis/Calculus/LineDeriv/IntegrationByParts): $\int f\, \partial_v g = -\int (\partial_v f)\, g$ for scalar $f, g$ with the three products integrable and differentiability on the supports.

Cross-seabed state (the user's hint): hironaka (v4.33.1, 356/360 leaves done) has the frozen and discharged statements `Statements.exists_logResolutionData_of_analyticOnNhd` (E4: chart form, `LogResolutionData` for $|K|$ at a zero of an analytic $K$ on open $U \subseteq \mathbb{R}^d$) and `exists_hasLLCExponentsOn_of_analyticOnNhd_nonneg` (E5: $\exists \lambda \in \mathbb{Q}_{>0}, \theta \in \{1..d\}$ with sublevel volumes $\asymp \varepsilon^\lambda(-\log\varepsilon)^{\theta-1}$ on small cubes, a Θ statement). greybook (v4.33.1, depends on hironaka) has `hasPreciseLaplace_of_hasPreciseRLCT_general` (Abelian transfer: precise sublevel asymptotics $C\varepsilon^\lambda(-\log\varepsilon)^m$ ⇒ precise Laplace asymptotics $C\Gamma(\lambda+1) t^{-\lambda}(\log t)^m$) and the whole Ch6 evidence/zeta machinery. laplace is on v4.33.0 with deps mathlib, Threepoint, resolution-common; consuming hironaka/greybook needs a toolchain bump and new dependencies (the strucdual→grammar precedent), and E5 is Θ-only while the greybook transfer wants the precise constant.

## Candidates v1 (Claude)

**A. The normalized singular identifiability theorem (the note's new IBP proof), in laplace.**
Statement (Lean-flavoured): for `L₁ L₂ : (ι → ℝ) → ℝ` with `ContDiff ℝ ∞ L₁`, `ContDiff ℝ ∞ L₂`, `0 ≤ Lᵢ`, `AnalyticAt ℝ Lᵢ p` and `Lᵢ p = 0` for all `p ∈ W₀`, and a scalar `C : ℝ → ℝ` such that for every `φ` with `ContDiff ℝ ∞ φ` and `HasCompactSupport φ`,
`SuperPoly (fun t ↦ ∫ w, φ w * exp (-(t * L₂ w)) - C t * ∫ w, φ w * exp (-(t * L₁ w)))`,
there is an open `U ⊇ W₀` with `L₁ = L₂` on `U` (`normalized_families_force_eq_near`), via a point form `normalized_families_force_germ_eq_at` (`∀ᶠ w in 𝓝 p, L₁ w = L₂ w`). Ingredients: (i) IBP lemma `∫ (fderiv φ v) e^{-tL} = t ∫ φ (fderiv L v) e^{-tL}` for smooth compactly supported `φ` and `C¹` `L`; (ii) cancellation of `C` between the two observables `∂_vφ` and `φ ∂_v L₁` (both in the class since `L₁` is smooth); (iii) the sector lower bound for the amplitude `a = ∂_v(L₂ - L₁)` against `K = L₂` (analytic `a` with nonzero germ, `K ≥ 0` `C²` with `K p = 0`), reusing Sector/LeadingPart/AnalyticBridge/SingularPrep; (iv) all directional derivatives eventually zero ⇒ `fderiv (L₂ - L₁) = 0` near `p` ⇒ locally constant (Mathlib mean-value lemma on a ball) ⇒ `0`; (v) the assembly of `pencil_families_force_eq_near`. Rationale: one coherent theorem, closes the note's only stated open question, all machinery present; it is the paper's headline change since August.
Expected closed form: none (structural); the IBP identity is checked numerically below.

**B. Forward direction at a singular point by cross-seabed bridge.** For analytic `K ≥ 0` on an open `U`, `K w = 0`, not identically zero near `w`: `∃ λ ∈ ℚ_{>0}, θ ∈ {1..d}`, `∫_{cube} e^{-tK} =Θ[atTop] t^{-λ} (log t)^{θ-1}`, from hironaka E5 plus a Θ-version of greybook's Abelian transfer. Rationale: the first germbij statement that uses resolution data; the note's sec:singular asserts the existence of the expansions via resolution. Cost: laplace toolchain bump to v4.33.1, two new lake dependencies, and a Θ-Abelian transfer that greybook does not have (it has the precise-constant one). Several independent new pieces → a programme, not a tide; proposed as the follow-up.

**C. Equal zero loci are forced (rem:singular-scope (a)), projective form.** Under the hypotheses of A with `L₁ p = 0` but `L₂ p ≠ 0` for some `p`: contradiction. Proof: bump `ψ` at `p` gives `∫ψ e^{-tL₁} ≳ t^{-d/2}` (quadratic upper bound) while `∫ψ e^{-tL₂} = O(e^{-δt})`, so `C t = o(t^{-∞})`, hence every `∫φ e^{-tL₂}` is beyond all orders, contradicting a bump at a zero of `L₂` (if `L₂` has one; if `W₀ ≠ ∅` it does). Small; a natural companion lemma to A (it lets A's hypothesis be weakened from "common zero set `W₀`" to "`W₀ ⊆ {L₁ = 0}` and `L₂` vanishes somewhere"). Possibly fold into A's tide as a second theorem.

Claude's preference: A (with C if time allows); B recorded as the next arc.

## GPT-6 Astra v1

Saved verbatim: `tide-log/gpt_germbij_normalized_v1.md` (prompt: `gpt_germbij_normalized_prompt_v1.md`). Summary: A is correct as a local germ-identifiability theorem. Refinements adopted: (i) the cancellation identity to formalise is $R_{D\phi}(t)/t - R_{\phi\, DL_1}(t) = I_2(\phi\, D(L_2 - L_1), t)$ with $R_\phi = I_2(\phi) - C\, I_1(\phi)$, so nothing is ever multiplied by $C$; (ii) the sector lemma needs only $K = L_2$ $C^2$, nonnegative, $K(p)=0$ and $a = Dg$ analytic, and $a(p) = 0$ holds automatically (both losses have a minimum at $p$), matching `exists_least_nonzero_diagonal`'s `g 0 = 0`; (iii) local constancy is a convex-ball statement, not global; (iv) global `ContDiff ℝ ∞` is a convenience, a support-local version is a later wrapper; (v) `C : ℝ → ℝ` may be arbitrary; (vi) stretch corollaries: `SuperPoly (C - 1)` after equality, and equal zero loci (candidate C, symmetric version needs the polynomial bound $|C| = O(t^{d/2})$ from a bump at a zero of $L_1$); (vii) B's first modular deliverable is a standalone Θ-Abelian transfer lemma (no hironaka dependency), greybook's precise transfer cannot consume a Θ premise.

## Vote
- Claude: candidate A (point theorem + locus wrapper; normalized-expectation corollary and the stretch corollaries if time allows)
- GPT-6 Astra: candidate A

## Numerical check

The only formula with numerical content is the directional integration-by-parts identity $\int \partial_y\phi\, e^{-tL} = t\int \phi\, \partial_y L\, e^{-tL}$. Checked with `scipy.integrate.quad` for $L = x^4$ (singular, $d=1$), $\phi = (1 + x/2)\, e^{-1/(1-x^2)}\mathbf{1}_{|x|<1}$, at $t = 10, 100, 1000$: relative differences $5\times10^{-11}$, $1.5\times10^{-15}$, $2.7\times10^{-15}$. The theorem itself is structural (an equality of germs); no closed form to check.

## Step 3 hand-off

File `Laplace/Multi/NormalizedSingular.lean` (imports `Laplace.Multi.SingularSmooth`, `Laplace.Anchoring`), registered in `Laplace.lean`. Lemmas in order: `SuperPoly.sub`, `SuperPoly.congr`, `SuperPoly.div_id`, `SuperPoly.bounded_mul`; `integral_fderiv_mul_exp` (IBP via `integral_mul_fderiv_eq_neg_fderiv_mul_of_integrable`, `f = φ`, `g = e^{-tL}`); `superPoly_integral_fderiv_sub_mul_exp` (the cancellation `R_{∂φ}/t − R_{φ ∂L₁} = I₂(φ ∂(L₂−L₁))`); `analytic_square_weight_eq_zero_near` (translate to 0, `exists_least_nonzero_diagonal`, `analytic_remainder_bound`, `leading_part_scaled_set`, `quadratic_upper_bound_of_nonneg`, a `ContDiffBump` with `rIn = R`, `sector_lower_bound_multi`, `setIntegral_le_integral`, `lower_bound_not_superpolynomial`); `eventually_eq_zero_of_fderiv_single_eventually_zero` (`Finite ι`, `Convex.is_const_of_fderivWithin_eq_zero` on a ball); `normalized_families_force_germ_eq_at`, `normalized_families_force_eq_near`, `normalized_expectations_force_eq_near`.

## Result

Commit `4a057b4` on `tide/germbij-normalized`; `lake build` clean, `scripts/sorries` 0/0/0/0. Theorems: `normalized_families_force_germ_eq_at`, `normalized_families_force_eq_near`, `normalized_expectations_force_eq_near`, plus the four lemmas above. Surprises: (i) the whole file landed in four LSP rounds; the only mathematical wrinkle was that the amplitude `∂_v(L₂ − L₁)` must vanish at `p` for `exists_least_nonzero_diagonal`, which holds because both losses have a local minimum there (`IsLocalMin.fderiv_eq_zero`); (ii) `fderiv_sub` on this Mathlib pin is stated for `Pi` subtraction and does not rewrite `fun w ↦ L₂ w − L₁ w`; use `(hasFDerivAt.sub hasFDerivAt).fderiv`; (iii) grade side conditions for `ContDiff ℝ ∞` (`∞ ≠ 0`, `∞ + 1 ≤ ∞`) are discharged by `by simp` / `contDiff_infty_iff_fderiv`, and `C²` at an analytic point comes free from `AnalyticAt.contDiffAt`, not from `ContDiffAt.of_le`.
