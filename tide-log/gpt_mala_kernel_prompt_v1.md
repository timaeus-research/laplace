You are consulted as GPT-6 Astra in a Lean 4 / Mathlib formalisation loop (the "Tide"). The attached tide log describes the seabed (Lean repo `laplace`, Mathlib pinned Sep 2026), the results of the previous tide (`mala-invariance`, Metropolis–Hastings invariance in `lintegral` form), and the candidates A (kernel packaging + `Measure.bind` fixed point), B (moments of the invariant Gaussian law: mean 0, covariance `P⁻¹`) and C (reversibility).

Questions:
1. Are A1–A4 and B correct as stated? In A1, is `μ.withDensity (fun y => ofReal (q x y * α x y) / Z) + (1 - a x) • Measure.dirac x` the right measure-level definition, and does `Measure.measurable_of_measurable_coe` (measurability of `x ↦ K x E` for every measurable `E`) suffice to build a `ProbabilityTheory.Kernel` (structure with fields `toFun`, `measurable'`)? Is `Kernel.withDensity` preferable, or does it bring extra hypotheses (`IsSFiniteKernel`, measurability of the density in both variables)?
2. A3: the cleanest way to state and prove "the target law is a fixed point" — `Measure.bind ν K = ν` via `Measure.ext fun E hE => by rw [Measure.bind_apply hE K.measurable.aemeasurable, …]`, or via `Kernel.const`/`Measure.comp`? Any pitfalls with `bind` requiring `SFinite`/`IsFiniteMeasure` instances, or with `AEMeasurable` of a kernel's coercion?
3. B: the bridge from `μ.withDensity (fun x => ofReal (π x) / T)` (an `ℝ≥0∞` density) to a real integral `∫ g x ∂ν = (∫ π x * g x) / T.toReal`: which Mathlib lemma is the right one — `integral_withDensity_eq_integral_toReal_smul` (needs `∀ᵐ x, f x < ∞`), `integral_withDensity_eq_integral_smul` (NNReal density), or going through `withDensity_ofReal`/`ENNReal.toReal_div`? And `(∫⁻ ofReal π).toReal = ∫ π` for integrable nonneg `π` is `integral_eq_lintegral_of_nonneg_ae` (plus `ENNReal.toReal_ofReal`)? Any integrability side conditions for `x i * x j` under the Gaussian law that I should discharge from the seabed's `integrable_coord_mul_gaussianWeight_matCLM` rather than reprove?
4. Sizing: is A + B (+ C) one tide (~300 lines)? Anything else close to this seabed worth including (e.g. `IsMarkovKernel` giving `IsProbabilityMeasure (ν.bind K)` for free, or stating the fixed point for the `n`-step kernel `K^n` by induction)?
Vote for a candidate set at the end (`Vote: A+B`, `Vote: A+B+C`, …) with wording qualifications.
