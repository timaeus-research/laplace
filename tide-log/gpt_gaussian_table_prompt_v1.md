You are consulted as GPT-6 Astra in a Lean 4 / Mathlib formalisation loop (the "Tide"). The attached tide log describes the seabed (Lean repo `laplace`, Mathlib pinned Sep 2026) and the candidates A (joint independence of the Gaussian projections `<u_i, ξ_{c,k}>` across chains, times and directions), B (the resulting `FourthMomentTable`), C (the unconditional E4 whole-covariance law for the ULA chain).

Questions:
1. Is A correct and is the route through `iIndepFun.hasGaussianLaw_pi` → `HasGaussianLaw.map_fun` (CLM collecting the projections) → covariance computation → `HasGaussianLaw.iIndepFun_of_covariance_inner` the right one? For the covariance of `<u_i, ξ_a>` and `<u_j, ξ_{a'}>`: (i) `a ≠ a'` — independence of `ξ a` and `ξ a'` gives zero covariance; which Mathlib lemma (`IndepFun.covariance_eq_zero`? `ProbabilityTheory.covariance_eq_zero_of_indepFun`?) and what integrability does it need? (ii) `a = a'`, `i ≠ j` — is `cov[fun ω => <u_i, ξ_a ω>, fun ω => <u_j, ξ_a ω>; P] = <u_i, u_j>` obtainable from `covarianceBilin_stdGaussian` / `variance_dual_stdGaussian` after transporting through `P.map (ξ a) = stdGaussian` (`covariance_map`?), or is it easier to compute the covariance as `∫ <u_i, ξ><u_j, ξ> - ∫<u_i,ξ> ∫<u_j,ξ>` with the seabed's `integral_inner_sq_stdGaussian`-style lemmas (polarisation: `<u_i, x><u_j, x> = ((<u_i+u_j, x>)² - (<u_i-u_j, x>)²)/4`)?
2. The index type: `HasGaussianLaw.iIndepFun_of_covariance_inner` is for `X : Π i, Ω → E i` — with the finite index `Fin C × Fin T × ι` and `E i = ℝ`, and the inner product on `ℝ` being multiplication. Is there friction in matching `⟪x, X i ω⟫` (for `x : ℝ`) with `x * X i ω`? Should A be stated for a general orthonormal family `u : ι → EuclideanSpace ℝ ι` (or `Orthonormal ℝ u`), and does `orthoCol` give `Orthonormal` conveniently from `orthoOf_transpose_mul`?
3. The finite window vs ℕ: `frobenius_realChain_le` (previous tide) takes `η : Fin C → ℕ → ι → Ω → ℝ` with a `FourthMomentTable` on `Fin C × ℕ × ι`. Options: (a) prove `iIndepFun` on the ℕ-indexed family from the finite-window statements (is there a Mathlib lemma "iIndepFun iff every finite subfamily is iIndepFun", e.g. `iIndepFun_iff_finset`-like, or via `iIndepFun.of_precomp` along injections?); (b) restate the chain theorems for `Fin T`-indexed innovations (padding with zeros beyond the window); (c) define the ℕ-indexed innovation family by extending the window with independent copies. Which is least painful?
4. Size: A+B+C ~400 lines? Any pitfalls with `HasGaussianLaw` instances for `Fin C × Fin T → EuclideanSpace ℝ ι` (needs `MeasurableSpace`, `BorelSpace`, `SecondCountableTopology` instances on the Pi type — all standard?), or with `iIndepFun.hasGaussianLaw_pi`'s exact hypotheses (each `ξ a` must `HasGaussianLaw`: from `P.map (ξ a) = stdGaussian` and `isGaussian_stdGaussian`)?
Vote for a candidate set at the end (`Vote: A+B+C`, …) with wording qualifications.
