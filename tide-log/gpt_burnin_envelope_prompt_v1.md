You are reviewing a candidate for one step ("tide") of a Lean 4 / Mathlib formalisation of the sampler-side mathematics of a note on Hessian-vs-sampling sanity checks for SGLD LLC estimation. The attached tide log states the seabed and candidates A–C with a numerical check. Please answer tersely:

1. Mathematics. Are A–C correct? Check (i) the identity `ρ^{2(b+1)}/((1−ρ²)(1+ρ)/2) = 2ρ^{2b}/(1−ρ)·(ρ/(1+ρ))²` and that each factor `ρ^{2b}`, `1/(1−ρ)`, `(ρ/(1+ρ))²` is increasing on `[0,1)` (so the product of nonnegative increasing factors is increasing); (ii) that with `ρᵢ = 1 − h pᵢ` the burn-in term `ρᵢ^{2(b+1)}/(N(1−ρᵢ²)(1 − h pᵢ/2))` is `(1/N)·[that function at ρᵢ]` because `1 − h pᵢ/2 = (1 + ρᵢ)/2`; (iii) the envelope bound: the per-direction term `(1+ρ²)/((1−ρ²)(1−hp/2)²)` is bounded by `τ(r²)/(1 − h pmax/2)²` using the two maxima separately (is there a sharper single-direction bound, or is the product-of-maxima the honest spectrum-free statement?); (iv) the final C and how it reads against the note's recipe ("`lr = 0.1/pmax`, at least `20κ` steps per chain"): with `h pmax = 0.1`, `τ(r²) ≈ 1/(h pmin) = 10κ`… is `τ(r²) = (1+r²)/(1−r²) ≈ 1/(1 − r) = 1/(h pmin) = 10κ`, or `20κ`? (note `τ(r) = (1+r)/(1−r) ≈ 2/(h pmin) = 20κ` is the autocorrelation time of the *chain*, while the variance envelope uses `τ(r²)`, the autocorrelation time of the *squared* chain — please confirm the factor 2 relationship so the log records it correctly).

2. Lean. For the monotone factors: `pow_le_pow_left₀`, `one_div_le_one_div_of_le`, `div_le_div_iff₀` + `nlinarith` for `ρ/(1+ρ)`; combining three nonnegative monotone factors with nested `mul_le_mul` — fine, or is there a `Monotone.mul` idiom for functions on a set? For the sums, `Finset.sum_le_sum` + `Finset.sum_const`/`Finset.card_univ`; for C, `le_trans` from the spectral theorem with `add_le_add` and `Real.sqrt_le_sqrt`, dividing by `d` via `div_le_div_of_nonneg_right`. Any pitfall?

3. Scope. A+B+C one tide (~200 lines)? Cheap valuable additions: the `κ`-form of C at `h pmax = 1/10` (substituting `h pmin = 1/(10κ)`), or the "effective sample size" reading `CN/τ_flat` of the Monte Carlo term (the note's "draw budget in units of `τ_flat`")?

End with a single line `Vote: <A+B|A+B+C|A+B+C+extras|other>` and one sentence of justification.
