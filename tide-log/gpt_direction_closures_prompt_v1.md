You are consulted as GPT-6 Astra in a Lean 4 + Mathlib (Sep 2026 pin) formalisation "tide". Read the attached tide log (Candidates v1, numerical check) and the attached Lean sources AR1.lean (the AR(1) chain structure, Gram table, `norm_sum_window_sq`, `sum_sum_pow_dist`, `pooled_sample_variance`) and ULA.lean (spectral packaging `orthoOf`, `spectral_real`, `ulaCov_conj_eq_diagonal`, `trace_mul_ulaCov`). Terse answers:

1. Correctness: (a) the shortfall bound `σ² − pooled ≤ σ² (ρ^{2(b+1)}/(N(1−ρ²)) + (1+ρ)/((1−ρ)CN))` from the closed form (check the two elementary bounds on `R₂` and `T_N` and the sign of `T_N − R₁²`); is `(1+ρ)/(1−ρ)` the right identification of the note's `τᵢ`, and is the bound tight up to the dropped `R₁²`? (b) the eigenbasis identity and the `γ_rel` form; is `‖u‖²/(tλ+γ)` right for a (not necessarily unit) eigenvector?

2. Lean route: (a) geometric sums — `∑_{i<N} (ρ²)^i ≤ 1/(1−ρ²)` for `0 ≤ ρ < 1` (`geom_sum_eq`, `div_le_div_of_nonneg_left`? or `geom_sum_Ico_le_of_lt_one`?); `∑_{m<N} (N − (m+1)) ρ^{m+1} ≤ N ∑_{m<N} ρ^{m+1} ≤ N ρ/(1−ρ)`; give the Mathlib names. (b) the inverse of `t • H + γ • 1` via `spectral_real`: `(t•H + γ•1) = U * diagonal (t λ + γ) * Uᵀ`, inverse `U * diagonal (t λ + γ)⁻¹ * Uᵀ` by `Matrix.inv_eq_right_inv` with `orthoOf_mul_transpose`/`orthoOf_transpose_mul` and `diagonal_mul_diagonal`; then `∑ i j, A i j * X i j = (A * Xᵀ).trace` and `Matrix.trace_mul_comm`. Pitfalls with `Matrix.smul_add`/`smul_one` inside the conjugation, or with `diagonal` of a function and `Matrix.diagonal_inv`? (c) `expected_pooled_sample_variance_ula` already exists as an integral identity; the shortfall corollary just rewrites its RHS — confirm nothing measure-theoretic is needed.

3. Better nearby candidates? One-line vote: A, B, C, or a modification.
