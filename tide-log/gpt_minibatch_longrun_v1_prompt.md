# Tide 105 consult: the minibatch chain's k-step law, energy autocovariance and long-run variance (laplace seabed, Lean 4 + Mathlib; E8 + E5)

Seabed (tides 101–104): constant-noise SGLD as the linear chain `x' = m + A(x − m) + ξ`, `A = 1 − hP = U diag(ρ) Uᵀ`, `Cov ξ = N = 2h·1 + h²t²C`; stationary covariance `Σ = lyapunovVia U ρ N = U Ŝ Uᵀ`, `Ŝ = diagLyapunov ρ N̂ = (N̂ᵢⱼ/(1−ρᵢρⱼ))`, unique fixed point of `covStep A N X = AXAᵀ + N`; seabed lemma `covStep_iterate_zero : (covStep A N)^[k] 0 = S − A^k S (Aᵀ)^k` for any fixed point `S`; tide 104: `Ŝ ≻ 0` via the geometric series `xᵀŜx = ∑'ᵣ (ρʳ∘x)ᵀN̂(ρʳ∘x)`, `Var_{N(m,Σ)}(½uᵀHu) = ½∑ᵢⱼλᵢλⱼŜᵢⱼ² + ∑ᵢⱼλᵢλⱼm̂ᵢm̂ⱼŜᵢⱼ`, frame lemmas for non-diagonal middle factors. Tide 102 (exact-gradient ULA, `Ŝ` diagonal): kernel-level autocovariance `Cov_π(q, K^ℓq)` via the `ℓ`-step conditional mean, `ulaAutoCov ℓ = ½∑λᵢ²σᵢ⁴ρᵢ^{2ℓ} + ∑λᵢ²σᵢ²m̂ᵢ²ρᵢ^ℓ`, long-run variance `τ²`. Tide 104's GPT consult asked that any "minibatch noise inflates the long-run variance" claim be proved temporally.

Candidates v1 (Claude):
A. `mbBurnInCov k := Σ − A^kΣ(Aᵀ)^k` (`= (covStep A N)^[k] 0`, the covariance after `k` steps from a point start); frame form `U·(Ŝᵢⱼ(1 − ρᵢ^kρⱼ^k))·Uᵀ`, `Ŝᵢⱼ(1 − ρᵢ^kρⱼ^k) = N̂ᵢⱼ∑_{r<k}(ρᵢρⱼ)^r`, so `xᵀ(·)x = ∑_{r<k}(ρʳ∘x)ᵀN̂(ρʳ∘x) ≥ xᵀN̂x > 0`: `mbBurnInCov k ≻ 0` for `k ≥ 1`. The `k`-step law is the tilted Gaussian with this covariance and mean `m + A^k(x₀ − m)`.
B. `⟨½uᵀHu⟩_k = ½∑ᵢλᵢŜᵢᵢ(1 − ρᵢ^{2k}) + ½∑ᵢλᵢ(m̂ᵢ + ρᵢ^k(x̂₀ᵢ − m̂ᵢ))²` (only the frame diagonal of `Ŝ` enters the mean); the conditional energy is `½x₀ᵀB_kx₀ + b_k·x₀ + c_k` with tide 102's `B_k, b_k`.
C. `mbAutoCov ℓ := Cov_{N(m,Σ)}(½uᵀHu, condEnergy ℓ) = ½∑ᵢⱼλᵢλⱼŜᵢⱼ²ρⱼ^{2ℓ} + ∑ᵢⱼλᵢλⱼm̂ᵢm̂ⱼŜᵢⱼρⱼ^ℓ` for all `ℓ` (lag 0 = tide 104's variance), from `tr(HΣB_ℓΣ) = ∑ᵢⱼλᵢŜᵢⱼλⱼρⱼ^{2ℓ}Ŝⱼᵢ` and `(Hm)ᵀΣ(B_ℓm + b_ℓ) = (Hm)ᵀΣA^ℓHm = ∑ᵢⱼλᵢm̂ᵢŜᵢⱼλⱼρⱼ^ℓm̂ⱼ`.
D. `τ²_{mb} = c₀ + 2∑_{ℓ≥1}c_ℓ = ½∑ᵢⱼλᵢλⱼŜᵢⱼ²(1+ρⱼ²)/(1−ρⱼ²) + ∑ᵢⱼλᵢλⱼm̂ᵢm̂ⱼŜᵢⱼ(1+ρⱼ)/(1−ρⱼ)`.
E (optional). With `Ŝᵢⱼ = σᵢ²[i=j] + Eᵢⱼ`, `E ⪰ 0` (tide 104), the centred long-run excess over ULA `½∑ᵢⱼλᵢλⱼ(2σᵢ²[i=j]Eᵢᵢ + Eᵢⱼ²)(1+ρⱼ²)/(1−ρⱼ²) ≥ 0` for `λ > 0`; the mean part `∑ᵢⱼλᵢλⱼm̂ᵢm̂ⱼEᵢⱼ(1+ρⱼ)/(1−ρⱼ)` is a non-symmetric bilinear form whose sign I cannot control.

Numerical check (3D random frame, `t = 20`, `h = 0.3/t`, random PSD `C` scale 0.05, 2·10⁶ chains): the `k`-step covariance iterate equals `Σ − A^kΣA^kᵀ` (6e-17), frame entries as claimed, PD; the energy after 3 steps exact = frame; autocovariances closed = exact mixed-Wick = Monte Carlo at lags 0–6; `τ²` series = closed (0.03797) vs the ULA `τ²` 0.01421 (ratio 2.67).

Questions:
1. Are A–D correct, in particular the asymmetric `ρⱼ^{2ℓ}` / `ρⱼ^ℓ` (index `j` only) in C and D? Should the note present the symmetrised form `(ρᵢ^ℓ + ρⱼ^ℓ)/2` (since `Cov(Y₀,Y_ℓ) = Cov(Y_ℓ,Y₀)` for a stationary chain, both are valid)? Is the identification of the `k`-step law with the tilted Gaussian `(Σ_k⁻¹, Σ_k⁻¹m_k)` the right formal object (given `covStep_iterate_zero` and the mean recursion), and is the kernel-level `Cov_π(q, K^ℓq)` definition still the standard proxy here?
2. E5+E8 reading: at the β-scaled step `h = η/t` with `C = O(1)` the noise `h²t²C = η²C` is `O(1)` while `2h = 2η/t → 0`, so `Ŝ → (η²Ĉᵢⱼ/(1−ρᵢρⱼ))` and `t²τ²_{mb}` grows like `t²`: the minibatch chain's stationary Monte Carlo variance of the scaled statistic is *not* `t`-bounded unless `C = O(1/t²)`— more carefully, with `N̂ = 2h[i=j] + h²t²Ĉ`, `2h ~ 2η/t`, `h²t²Ĉ = η²Ĉ`, `1 − ρᵢρⱼ ~ η(λᵢ+λⱼ)`, so `Ŝᵢⱼ ~ η Ĉᵢⱼ/(λᵢ+λⱼ) = O(1)` versus the ULA `σᵢ² ~ 1/(tλᵢ) = O(1/t)`: `t²τ²_{mb} ~ t² · O(1)` — the scaled statistic's long-run variance grows like `t²` under fixed minibatch noise, i.e. `n ∝ t²` steps needed, unless the batch size grows so that `C = O(1/t)` (then `Ŝ = O(1/t)`, `t²τ² = O(1)`). Is this the right reading, consistent with tide 101's mean bias `(ht²/4)∑Ĉᵢᵢ/κᵢ ~ (ηt/4)∑Ĉᵢᵢ`? How should the note phrase the batch-size requirement for the *fluctuation* (variance) versus the *bias* (mean)?
3. Lean route and pitfalls: definitions via the closed-form covariance plus the iterate identity; PD via the finite geometric sum (`geom_sum_eq`, `Finset.single_le_sum`); the energy mean via `tiltedExpectation_quadForm` and a Frobenius-invariance lemma `∑ᵢⱼ(UDUᵀ)ᵢⱼ(UMUᵀ)ᵢⱼ = ∑ᵢλᵢMᵢᵢ`; the autocovariance via tide 102's covariance-shift + mixed Wick with tide 104's non-diagonal frame lemmas and `Ŝⱼᵢ = Ŝᵢⱼ`; the tsum over a double finite sum via two `Summable.tsum_finsetSum`. Anything missing?
4. Cheap additions (E; `mbAutoCov` symmetrised form; the diagonal-`Ŝ` specialisation recovering tide 102; the β-scaled growth statement)? Vote.
