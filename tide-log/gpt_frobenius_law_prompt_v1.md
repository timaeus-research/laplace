You are consulted as GPT-6 Astra in a Lean 4 / Mathlib formalisation loop (the "Tide"). The attached tide log describes the seabed (Lean repo `laplace`, Mathlib pinned Sep 2026), the just-proved moment-form Wick identity for linear combinations of a `FourthMomentTable`, and the candidates A (Frobenius decomposition), B (per-entry variance of the pooled second-moment matrix under a four-moment table across chains × times × directions), C (the E4 whole-covariance law, exact and stationary envelope), D (follow-up: Gaussian discharge).

Questions:
1. Are B and C correct as stated? Check in particular: (i) the reduction of `Var(Σ̂_ij)` to the Gram table through `Cov(XY, ZW) = E[XZ]E[YW] + E[XW]E[YZ]` — the `(1 + δ_ij)` factor and the vanishing of cross-chain and cross-direction terms; (ii) the exact zero-start formula with `G_i(k,l) = s2_i(ρ_i^{|k-l|} - ρ_i^{k+l})` over the window `k, l ∈ {b+1, …, b+N}`; (iii) the envelope via `0 ≤ G_i(k,l) ≤ s2_i ρ_i^{|k-l|}` and the Toeplitz bound at `ρ_i ρ_j` (which needs `0 ≤ ρ_i ρ_j < 1`); (iv) the relative form and the `(d+1) τ(ρ²)/(CN)` special case. Should the statement target `E Σ̂` (variance only, as in C) or the stationary `Σ_∞ = diag(s2_i)` (adding the zero-start bias `∑_ij (E Σ̂_ij - Σ_∞,ij)^2`, which is diagonal and explicit)? Which reads best against the note's E4 finding?
2. Lean architecture for B: the realised chains along direction `i` for chain `c` are `realChain (ρ i) (fun k => η c k i)` (a ℕ-indexed innovation sequence per `(c, i)`). The `FourthMomentTable` must be a single family `g : Fin C × ℕ × ι → Ω → ℝ`. Is it cleaner to (a) take `η : Fin C → ℕ → ι → Ω → ℝ` and `g := fun a => η a.1 a.2.1 a.2.2`, with `IsLinComb g (univ ×ˢ range (T+1) ×ˢ univ) (realChain (ρ i) (η c · i) k)` by the same induction as `isLinComb_realChain`, or (b) state B for an abstract family of L⁴ functions `x : Fin C → ι → ℕ → Ω → ℝ` with hypotheses `IsLinComb g s (x c i k)` and a Gram-table hypothesis `∫ x c i k * x c' j l = if c = c' ∧ i = j then G i k l else 0`, and derive the realChain instance separately? (b) seems to isolate the combinatorics; is that your recommendation?
3. The double sum over `(c, k)` and `(c', l)` with the Kronecker deltas: after `Finset.sum_product`, the `c = c'` collapse via `Finset.sum_ite_eq` — any idiom for the `(1 + δ_ij)` bookkeeping (`if i = j then 2 else 1`)? For the Frobenius decomposition A, is it worth stating with Mathlib's `variance`/`covariance` API or with explicit integrals `∫ S² - (∫ S)²` (the seabed uses the explicit form throughout)?
4. Size: is A + B + C one tide (~500 lines)? Anything to drop or add? For D, confirm the plan: restrict to a finite time window `Fin T`, get joint Gaussianity of `(ξ_{c,k})_{(c,k)}` from `iIndepFun.hasGaussianLaw_pi`, push through the CLM `(ξ_a)_a ↦ (⟨u_i, ξ_a⟩)_{(a,i)}` with `HasGaussianLaw.map_fun`, compute the covariances (`covarianceBilin_stdGaussian` for `a = b`, independence for `a ≠ b`) and conclude with `HasGaussianLaw.iIndepFun_of_covariance_inner`.
Vote for a candidate set at the end (`Vote: A+B+C`, …) with wording qualifications.
