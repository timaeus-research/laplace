You are consulted as GPT-6 Astra in a Lean 4 + Mathlib (Sep 2026 pin) formalisation "tide". Read the attached tide log (Candidates v1, numerical check) and the attached excerpts: Defs.lean (the seabed's `PotentialApprox`/`ObservableApprox`/`ObservableJetApprox`/`ObservableTensorApprox` packages, `quadForm`, `trASig`, `tensorContractMatrix`, `gibbsExpectation`) and GaussianWickPosDef.lean (the discharged packages `laplaceCovHypotheses_matCLM` etc.). Terse answers:

1. Correctness: (a) confirm that specialising `gibbsExpectation_first_order_rate_explicit` to `φ = ½ quadForm (matCLM P)`, `a = 0`, `A = matCLM P` gives `2t⟨K⟩ → trASig (matCLM P) (matCLM P⁻¹) = card ι`, i.e. the note's `t⟨K⟩ → d/2`, with an `O(1/t)` rate, for every `V` with `PotentialQuinticApprox V (matCLM P)`; (b) confirm the sign/normalisation link between the note's `eq:mean` term `−½ S (tT:S)` (`S = (tH)⁻¹`) and the seabed's `−½ Hinv (T:Hinv)` for `t⟨x⟩` (so `⟨x⟩ − w* ≈ −½ S (tT:S)`), and that on the quadratic valley (`T_zzz = 6abp`, `T_zzw = −2ab`, `H = [[1+ap², −ap],[−ap, a]]`) this gives exactly `(0, b/t)`.

2. Lean route for `quadObservable`: the fields to fill are (base) `phi_continuous`, `phi_zero`, `local_radius`, `local_const`, positivity/nonnegativity, `local_bound : ‖w‖ ≤ R → |φ w − ⟨a, w⟩| ≤ C ‖w‖²` and possibly a growth field; (jet) `qφ`, `qφ_continuous`, `qφ_even`, `qφ_bound_const`, `qφ_bound : |qφ w| ≤ C ‖w‖²`, `jet_radius`, `jet_const`, jet remainder bound; (tensor) `A`, `A_symm : dot u (A v) = dot v (A u)`, `qφ_eq_A_diag`, `Φ := 0`, `Φ_symm`, `Φ_jet_bound`. The load-bearing inequality is `|∑ i, w i * (P *ᵥ w) i| ≤ (∑ i, ∑ j, |P i j|) * ‖w‖ ^ 2` in the Pi sup norm on `ι → ℝ`. Give the 6–10 line Mathlib idiom (`norm_le_pi_norm`, `Finset.abs_sum_le_sum_abs`, `abs_mul`, `mul_le_mul`, `Finset.sum_le_sum`, …) and any pitfall with `‖w‖ ^ 2` versus `‖w‖ * ‖w‖`. Is `ContinuousMultilinearMap` zero's symmetry `Φ_symm` just `by simp`? Any field I am likely missing (e.g. `HasPolyGrowth` on the observable)?

3. Better nearby candidates I missed? (E.g. also `Cov[K, ψ]` (`eq:covK`) specialised to `φ = K`, `a = 0` — which the explicit theorem's `h_phi_grad_zero : a = 0` was designed for; the coefficient `cov2Coefficient` would then need its own closed form.) One-line vote: A, B, C, or a modification.
