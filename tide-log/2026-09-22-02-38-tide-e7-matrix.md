# Tide: e7-matrix

**Direction (user):** Just proceed on auto. You can bite off big chunks here to formalise and don't worry about retrospectives.
(Auto-mode continuation: E7 in the note's matrix form, closing the E7 arc after `order3-parity`.)
**Seabed:** laplace, commit a5fdc8f
**Started:** 2026-09-22T02:40Z

## Candidates v1 (Claude)

See `gpt_e7_matrix_prompt_v1.md` (A–C verbatim).

- A: the seabed's `oneLoopCov t H T Q4` on the rotated tensors `H = Q diag(λ) Qᵀ`, `Tᵢⱼₖ = ∑ₗ αₗ Qᵢₗ Qⱼₗ Qₖₗ`,
  `Q4ᵢⱼₖₘ = ∑ₗ γₗ Qᵢₗ Qⱼₗ Qₖₗ Qₘₗ` equals `Q diag(1/(λᵢt) + Cᵢ/t²) Qᵀ`, `Cᵢ = αᵢ²/λᵢ⁴ − γᵢ/(2λᵢ³)`
  (`contractQ = Q diag(γ/(λt)) Qᵀ`, `bubble = tadpoleLine = Q diag(α²/(λt)²) Qᵀ`).
- B: relative Frobenius error of the exact covariance of the rotated oscillator against `oneLoopCov`:
  `‖Cov_t − oneLoopCov‖²_F / ‖(tH)⁻¹‖²_F ≤ K/t⁴` for `t ≥ T`.
- C: the contrast with Laplace alone, `t² ‖Cov_t − (tH)⁻¹‖²_F/‖(tH)⁻¹‖²_F → (a² − ½)²` (seabed), packaged as "the one-loop term
  improves the relative Frobenius error from `Θ(1/t)` (when `a² ≠ ½`) to `O(1/t²)`".

## GPT-6 Astra v1

Saved verbatim in `gpt_e7_matrix_v1.md`. Summary: A correct (all four contractions checked through `D = QᵀSQ`; `bubble = tadpoleLine`
holds for these tensors, not in general; keep `t ≠ 0` for the closed forms); the derivative identification of `rotT`, `rotQ` with E2's
tensors at `c` is right (the `1/6`, `1/24` cancel on differentiation). Correction to C: in the note's parametrisation
`Cᵢ = αᵢ²/λᵢ⁴ − γᵢ/(2λᵢ³) = (a² − ½)/λᵢ`, not `/λᵢ²`, so `oneLoopCov = (1 + (a² − ½)/t) S`. Lean: factor everything through `D`, prove
the contractions for arbitrary diagonal `s` first, assemble `Π` and `S Π S` by matrix algebra (`(Q diag x Qᵀ)(Q diag y Qᵀ) = Q diag(xy) Qᵀ`),
not entrywise. B: squared relative error `≤ K/t⁴` with `K = ∑Kᵢ²/∑λᵢ⁻²` and the Laplace denominator is the right normal form; require
`0 < d`. C: `rel²(Laplace) ≥ (a² − ½)²/(2t²)` eventually when `a² ≠ ½`; do not claim a sharp `Θ(t⁻²)` for the one-loop error; at
`a² = ½` Laplace itself is `O(t⁻²)`. Vote: A+B+C, B the headline.

## Vote
- Claude: A+B+C
- GPT-6 Astra: A+B+C

## Numerical check

`numcheck_e7_matrix.py` (`d = 3`, `λ = (1, 2, 5)`, `a = ½`, random orthogonal `Q`): `‖oneLoopCov − Q diag(1/(λt) + C/t²) Qᵀ‖_F = 1.7e-16`
at `t = 7` with `contractQ`, `bubble`, `tadpoleLine` each matching their closed forms to `1e-16`; `t²‖Cov − oneLoopCov‖_F/‖S‖_F =
0.206, 0.238, 0.255, 0.264, 0.269` at `t = 20 … 320` (bounded) while `t‖Cov − S‖_F/‖S‖_F → 0.25 = |a² − ½|`.

## Result

Commit `97db24a` on `tide/e7-matrix`; `lake build` clean, `scripts/sorries` 0/0/0/0. A, B and C all landed.
`Laplace/Multi/OneLoopRotated.lean` (     467 lines): `rotT`, `rotQ`, `sum_comm3`, `col_orthonormal`, `conj_diagonal_apply`,
`conj_mul_conj`, `conj_add`, `conj_sub`, `conj_smul`, `transpose_conj_diagonal`, `contract_conj_diagonal`,
`smul_conj_diagonal_inv`, `rotT_contract`, `rotT_contract1`, `rotQ_contract`, `contractQ_rot`, `contractT_rot`,
`contract_conj_diagonal_sq`, `bubble_rot`, `tadpoleLine_rot`, `oneLoopPi_rot`, `oneLoopCov_rot`,
`frobenius_rel_oneLoop_rotatedAnharmonic`, `oneLoopCov_rot_note`, `oneLoopCov_rot_half`, `frobenius_rel_laplace_lower`.

Surprises: every tensor contraction reduces to the single eigenframe identity `∑ₖₘ Qₖₚ Sₖₘ Qₘ_q = δₚ_q sₚ`, and the two cubic
one-loop diagrams (bubble and tadpole-on-the-line) coincide for these tensors; in the note's parametrisation the whole one-loop
correction is the scalar factor `1 + (a² − ½)/t`.
