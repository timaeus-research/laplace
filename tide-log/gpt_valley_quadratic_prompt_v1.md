You are consulted as GPT-6 Astra in a Lean 4 + Mathlib (Sep 2026 pin) formalisation "tide". Read the attached tide log (Candidates v1, with numerical check) and the attached Lean sources: Rosenbrock.lean (the seabed's exact Rosenbrock moments via the shear `valley μ g a` machinery, coefficient matrices `Fin 5 → Fin 5 → ℝ` over the shear coordinates `z = x − μ`, `u = y − g x`) and OneLoop.lean (the one-loop functional and its Rosenbrock exactness, previous tide). Terse answers:

1. Correctness: check the general closed forms in A.1–A.3 (moments, `Var y = p²/t + 2b²/t² + 1/(at)`, `H`, `(tH)⁻¹`, `T`, `Q`, `Π = 2a²b²(p,−1)(p,−1)ᵀ`), in particular the mixed moment `⟨xy⟩ = μg(μ) + (μb + p)/t` and `⟨y²⟩ = g(μ)² + (p² + 2g(μ)b)/t + 3b²/t² + 1/(at)`. Also confirm the bubble `TSST = (1/t²)!![4a²b²p² + 8ab², −4a²b²p; −4a²b²p, 4a²b²]` and tadpole-line `(4ab²/t²) e_z e_zᵀ`.

2. Lean route: I will copy the Rosenbrock proofs with symbolic coefficient matrices (entries like `μ * (b*μ^2 + c*μ + e)`), `hφ` closed by `simp only [Fin.sum_univ_five, quadFn, valleySlope]; simp; ring`, and then `field_simp; ring` for the closed forms. Any pitfall you foresee with symbolic (rather than numeral) coefficient entries under `simp` + `ring` — e.g. `ring` timeouts on ⟨y²⟩ with 25 coefficient terms, or `field_simp` needing `a ≠ 0`, `t ≠ 0` explicitly? Would you state the results for `valley μ (fun x => b*x^2 + c*x + e) a` directly or through a named `quadFn b c e`, given that `simp` must see through it in `hφ` but `rw` lemmas about `valley μ (quadFn …) a` must match syntactically?

3. Is there a stronger nearby statement worth adding at low cost (e.g. `Var L = 1/t²` for every quadratic valley; the third cumulant of `y`, `κ₃(y) = 8b³/t³`?; the statement for a general direction `v`)? End with a one-line vote: A, B, C, or a modification.
