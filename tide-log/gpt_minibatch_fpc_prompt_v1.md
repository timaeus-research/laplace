You are consulted as GPT-6 Astra in a Lean 4 + Mathlib (Sep 2026 pin) formalisation "tide". Read the attached tide log (Candidates v1, numerical check) and the attached Lean source FullStep.lean (the seabed's abstract E8 full law: `stateTerm D X = ∑ i, D i * X * (D i)ᵀ`, `fullStep`, `minibatchCoeff h t m n = h^2 * t^2 * (1 - m/n) / (m * n)`, `e8FullStep`). Terse answers:

1. Correctness. (a) Confirm the finite population correction in bilinear form: for uniform `m`-subsets `B` of `n` items, `E_B[β(mean_B a − ā, mean_B a − ā)] = ((1 − m/n)/(m(n−1))) ∑ᵢ β(aᵢ − ā, aᵢ − ā)` for any bilinear `β` (so `C_g = (1/m)(1 − m/n) S²` with the `n−1` sample covariance, and the full-law coefficient is `(1 − m/n)/(m(n−1))`, not `(1 − m/n)/(mn)` as `minibatchCoeff` currently has). Also confirm the counting `#{B ∋ i} = C(n−1,m−1)`, `#{B ∋ i,j} = C(n−2,m−2)` and the identity route `∑_B ∑_{i,j∈B} g i j = C(n−1,m−1)∑ᵢ g i i + C(n−2,m−2) ∑_{i≠j} g i j`. (b) Is the note's usage (`S²` = sample covariance with `n−1`, `C_g = (1/m)(1−m/n)S²`) consistent with `(1 − m/n)/(m(n−1)) ∑ᵢ (gᵢ − ḡ)(gᵢ − ḡ)ᵀ`? (c) Edge cases: `m = n` (coefficient `0`, `B = univ`), `n = 1`.

2. Lean route. I plan `Finset.powersetCard m (Finset.univ : Finset (Fin n))`, `Finset.card_powersetCard`, and Mathlib's `Finset.card_filter_powersetCard_subset (s t) (n) (hst : s ⊆ t) (hsn : #s ≤ n) : #((t.powersetCard n).filter (s ⊆ ·)) = Nat.choose (#t − #s) (n − #s)` with `s = {i}` and `s = {i, j}`; the double counting `∑_B ∑_{i∈B} f i = ∑_i f i * #{B ∋ i}` via `Finset.sum_comm'`/`Finset.sum_filter`/`Finset.sum_boole`. Give the 5–10 line idioms for (i) `∑ B ∈ powersetCard m univ, ∑ i ∈ B, f i = C(n−1,m−1) • ∑ i, f i` (which `sum_comm` variant handles a dependent inner range, and how to rewrite `∑ i ∈ B, f i` as `∑ i, if i ∈ B then f i else 0`), and (ii) the binomial ratios `C(n−1,m−1)/C(n,m) = m/n`, `C(n−2,m−2)/C(n,m) = m(m−1)/(n(n−1))` as real numbers (`Nat.succ_mul_choose_eq`, `Nat.choose_mul_succ_eq`, casting pitfalls with `n − 1` in ℕ vs ℝ). Should the theorem be stated for general `n` with `1 ≤ m ≤ n` and `2 ≤ n`, or for `n + 2` and `m + 1` to avoid ℕ-subtraction?

3. For the bilinear map, `β : V →ₗ[ℝ] V →ₗ[ℝ] W` (then `map_add`, `map_smul` via `LinearMap.map_add₂`, `LinearMap.map_smul₂`?) versus stating the identity with two explicit hypotheses; and the instances `vecMulVec` (as `LinearMap.mk₂`) and `fun X Y => X * Σ * Y` on matrices. Any pitfall? Better nearby candidate? One-line vote: A, B, C, or a modification.
