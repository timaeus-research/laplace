# Tide: e2-matrix

**Direction (user):** Just proceed on auto. You can bite off big chunks here to formalise and don't worry about retrospectives.
(Auto-mode continuation: eq:mean and eq:covK in the note's matrix notation for E2's oscillator, chained off `tide/e7-matrix`.)
**Seabed:** laplace, branch `tide/e7-matrix` (main a5fdc8f + the e7-matrix commit)
**Started:** 2026-09-22T03:12Z

## Candidates v1 (Claude)

See `gpt_e2_matrix_prompt_v1.md` (A–C verbatim).

- A: eq:mean at `γ = 0`, `meanShift t H T = −(t/2) S (T:S)`; on the rotated tensors `= Q (−α/(2λ²t))`; `|⟨wⱼ⟩ − cⱼ − meanShift j| ≤ K/t²`.
- B: eq:covK's four terms as `covKMatrix t H T B b`; on the rotated tensors `= (∑ᵢ ((QᵀBQ)ᵢᵢ/(2λᵢ) − (Qᵀb)ᵢαᵢ/(2λᵢ²)))/t²`; for the
  ambient probe `ψ(w) = ½(w−c)ᵀB(w−c) + bᵀ(w−c)`, `t²(Cov[L∘A, ψ] − covKMatrix) → 0`.
- C (optional): the note's parametrisation.

## GPT-6 Astra v1

Saved verbatim in `gpt_e2_matrix_v1.md`. Summary: A correct (sign and factor of `meanShift`; call the bound an absolute `O(t⁻²)`, not
"relative `O(1/t)`", since a coordinate's leading coefficient can vanish); B's four-term evaluation correct (`+½ − ½ − ½ = −½` on the
cubic terms; `B` need not be symmetric; the probe identity needs only `QQᵀ = 1`) **but** the four-term expression scales like `1/t²`
and therefore predicts `Cov_t[L, ψ]`, whereas the note writes `Cov[K, ψ]` with `K = tL`: as written eq:covK is off by the factor `t`
(Gaussian diagnostic: `Cov[L, ψ] = ½ tr(HSBS)`, `Cov[tL, ψ] = (t/2) tr(HSBS)`). Name the functional accordingly and expose both
`t²(Cov[L, ψ] − covLMatrix) → 0` and `t Cov[K, ψ] → C`. Lean: `tr(HSBS) = tr((SHS)B)` by `trace_mul_cycle` reuses the `SHS` identity;
package `(QDQᵀ)(Qv) = Q(Dv)`, `(Qv)⬝(Qw) = v⬝w`, `b⬝(Qv) = (Qᵀb)⬝v`; mind `mulVec_mulVec`'s direction. No `O(t⁻³)` residual claim
(no rate available). Vote: A+B with the normalisation correction mandatory; C optional.

## Vote
- Claude: A+B (+ the `K = tL` normalisation remark)
- GPT-6 Astra: A+B (C optional)

## Numerical check

`numcheck_e2_matrix.py` (`d = 3`, `λ = (1, 2, 5)`, `a = ½`, random `Q`, `c`, symmetric `B`, `b`): `‖meanShift − Q(−α/(2λ²t))‖ = 1.8e-17`
at `t = 9`; `t² covKMatrix = −0.97104` equals the diagonal formula to 15 digits; exact `t² Cov[L, ψ] = −0.9500, −0.9657, −0.9697` at
`t = 50, 200, 800`, converging like `1/t`; `t‖Q⟨u⟩ − Q(−α/(2λ²t))‖ = 6.5e-3, 1.7e-3, 4.2e-4` (like `1/t`).
