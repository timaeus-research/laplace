# Tide: covK-rate

**Direction (user):** Just proceed on auto. You can bite off big chunks here to formalise and don't worry about retrospectives.
(Auto-mode continuation: E2's "relative error ∝ 1/t … including for eq:covK", now reachable from the sharp moment rates.)
**Seabed:** laplace, branch `tide/moments-sharp` (main df8edde + the moments-sharp commits b2fd05c, 35676d3)
**Started:** 2026-09-22T03:49Z

## Candidates v1 (Claude)

See `gpt_covK_rate_prompt_v1.md` (A–C verbatim).

- A: rates for the six pair covariances `t² Cov[x^m, x^n]`, `(m, n) ∈ {2,3,4} × {2,1}`, from the sharp moment rates and a
  product-of-rates lemma.
- B: `|t² Cov[ℓ, (B/2)x² + bx] − (B/(2λ) − bα/(2λ²))| ≤ K/t`, i.e. `|Cov − covKOneDim| ≤ K/t³`.
- C: separable diagonal, off-diagonal `|t² Cov_L[L, uᵢuⱼ]| ≤ K/t`, full quadratic probe, and `|Cov[L∘A, ψ] − covKFormula| ≤ K/t³`.

## GPT-6 Astra v1

Saved verbatim in `gpt_covK_rate_v1.md`. Summary: all six pair constants correct; organise A around the normalised moments
`Z₁ = t⟨x⟩, Z₂ = t⟨x²⟩, Z₃ = t²⟨x³⟩, Z₄ = t²⟨x⁴⟩, Z₅ = t³⟨x⁵⟩, Z₆ = t³⟨x⁶⟩` (all `a_j + O(1/t)`) with the table
`t²Cov[x²,x²] = Z₄ − Z₂²`, `t²Cov[x³,x²] = (Z₅ − Z₃Z₂)/t`, `t²Cov[x⁴,x²] = (Z₆ − Z₄Z₂)/t`, `t²Cov[x²,x] = Z₃ − Z₂Z₁`,
`t²Cov[x³,x] = Z₄ − Z₃Z₁/t`, `t²Cov[x⁴,x] = (Z₅ − Z₄Z₁)/t`; pointwise helper lemmas (sum, scalar, product, rated-to-bounded,
bounded-over-`t`); the even order-2 theorem is not needed. `ring` does not cancel `t`: use `(eq_div_iff ht).2` then `ring`. Rotated
`K/t³` absolute statement is the headline; relative form only with `C_d ≠ 0`; read E2's "∝ 1/t" as an `O(1/t)` upper bound. Vote:
A+B+C as one tide, staged.

## Vote
- Claude: A+B+C
- GPT-6 Astra: A+B+C (staged)

## Numerical check

`numcheck_covK_rate.py` (`λ = 2`, `a = ½`, `B = 3`, `b = 1.5`): `t·(t²Cov[ℓ, ψ] − C) = 0.227, 0.243, 0.252, 0.256, 0.258` at
`t = 50 … 800` (bounded); the six pair quantities `t·(t²Cov[x^m, x^n] − c_mn)` converge to `−0.127, −1.32, 1.50, 0.666, −0.299, −1.41`;
`d = 2` rotated (random `Q, c, B, b`): `t·(t² Cov[L∘A, ψ] − C_d) = 0.102, 0.087, 0.080, 0.076, 0.074`.
