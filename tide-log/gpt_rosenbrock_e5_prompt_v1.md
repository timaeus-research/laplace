You are consulted as GPT-6 Astra in a Lean 4 / Mathlib formalisation loop (the "Tide"). The attached tide log describes the seabed (Lean repo `laplace`, Mathlib pinned Sep 2026) and candidates A–D: exact corollaries of the closed-form 2D Rosenbrock covariance `rosenCov a t = (tH)⁻¹ + (2/t²) e_y e_yᵀ`, `(tH)⁻¹ = (1/t) !![1, 2; 2, 4 + 1/a]`, for the note's E5 findings.

Questions:
1. Check the arithmetic: A `wᵀ(tH)⁻¹w = 1/(at)` and the factor `1 + 2a/t` along `w = (2, -1)`; B `fᵀ(tH)⁻¹f = (25 + 4/a)/t`, factor `1 + 8/(t(25 + 4/a))` along `f = (1, 2)`; C `‖Δ‖_F² = 4/t⁴`, `‖(tH)⁻¹‖_F² = (9 + (4 + 1/a)²)/t²`; the eigenvector remark `H w = 10a w + (4, 0)`. Is `(2, -1)` the right proxy for the note's "stiff direction" (the eigenvector of `H = !![1 + 4a, -2a; -2a, a]` with the large eigenvalue), and is the discrepancy with the exact eigen-direction ratio (`1.19968` vs `1.2` at `a = 100`, `t = 1000`) worth stating as an `O(1/a)` remark? Note the seabed's `rosenbrock` carries the factor `1/2` (so the note's `200/t` is `2a/t`).
2. Statement style: should A be stated as an equality of the ratio (`wᵀ rosenCov w = (1 + 2a/t) * wᵀ (tH)⁻¹ w`) or as the two closed forms separately? For C, without Mathlib's Frobenius norm on matrices, is `∑ i j, (M i j)^2` the right explicit form (the seabed has no matrix-norm API), and is there a Mathlib `Matrix.frobenius_norm` worth connecting to?
3. D: `t * gibbsExpectation (rosenbrock a) t (rosenbrock a) = 1` from `gibbsExpectation_quadValley_self` via `rosenbrock_eq_quadValley` — anything to add (e.g. the per-direction split `½ + ½`, or that this is `d/2` with `d = 2` — the "Morse but not Laplace-accurate" point)?
4. Anything else close to this seabed for E5 (e.g. the `d`-dimensional Rosenbrock chain `∑ᵢ [a(xᵢ₊₁ - xᵢ²)² + (1 - xᵢ)²]/2` is *not* a quadratic valley in the seabed's sense — is any part of E5's `d = 10, 50` findings formalisable cheaply, or should this stay 2D)?
Vote for a candidate set at the end (`Vote: A+B+C+D`, …) with wording qualifications.
