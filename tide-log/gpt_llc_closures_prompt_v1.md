You are consulted as GPT-6 Astra in a Lean 4 / Mathlib formalisation loop (the "Tide"). The attached tide log describes the seabed (a Lean repo `laplace`, Mathlib pinned Sep 2026) and three candidate targets A, B, C. Seabed facts: `ulaStep P h = 1 - h • P`; `minibatchNoise h t C = (2h) • 1 + (h^2 t^2) • C`; `lyapunovVia U a N` is the Lyapunov solution through the spectral decomposition, with `lyapunovVia_conj_apply : (Uᵀ (lyapunovVia U a N) U) i j = (Uᵀ N U) i j / (1 - a i * a j)`; `minibatchCov_conj_diag : (Uᵀ Σ_mb U) i i = (1 + h t^2 (Uᵀ C U) i i / 2)/(p_i (1 - h p_i/2))` for `0 < h`, `0 < p_i`, `h p_i < 2`; `orthoOf_transpose_mul_mul : Uᵀ P U = diagonal p`; `trace_mul_ulaCov : tr(P Σ_ULA) = ∑ 1/(1 - h p_i/2)` proven by `trace_mul_cycle` after conjugating both factors; `ula_llc : t/2 tr(H Σ_ULA) = 1/2 ∑ 1/(1 - h p_i/2)` for `P = t • H`; `spectral_real : P = U D Uᵀ`; `orthoCol hQ i = toLp 2 (fun j => U j i)` with `mulVec_orthoCol : Q *ᵥ u_i = p_i • u_i`, `norm_orthoCol`; `inner_euclid_eq_sum : ⟨x, euclid H x⟩ = ∑ i j, H i j * (x i * x j)`; `ulaChain Q h ξ = vecChain (euclid (1 - h•Q)) (√(2h)) ξ` with `inner_ulaChain_eq_realChain : ⟨u, ulaChain Q h ξ k ω⟩ = realChain (1 - h p) (projNoise h u ξ) k ω` for an eigenvector `u`; the realised chain's L² Gram table `norm_x_sq : ‖x k‖² = v/(1-ρ²) (1 - ρ^(2k))` through `toAR1Chain`/`inner_toLp_eq_integral`; `expected_pooled_sample_variance_ula` as the template for pooled expectations.

Questions:
1. Are A, B, C correct as stated? In particular (A) the two-sided bound needs `c_i = (Uᵀ C U)_ii ≥ 0` (from `C` PSD) and `∑ c_i = tr C`; is the note's `C_g` normalisation (injected noise `2 lr I + lr² t² C_g`) consistent with `minibatchNoise h t C` so that the inflation is `(h t²/4) ∑ c_i/(1 - h p_i/2)`? (B) check the running-mean formula `1/2 ∑_i (1 - ρ_i^{2(b+1)}(1 - ρ_i^{2N})/(N(1-ρ_i²)))/(1 - h p_i/2)` and that `s2_i λ_i t = 1/(1 - h p_i/2)` with `s2_i = 2h/(1-(1-hp_i)²)`, `p_i = t λ_i`.
2. Lean route for `⟨x, H x⟩ = ∑_i λ_i ⟨u_i, x⟩²` with `u_i` the columns of the orthogonal `U` from `spectral_real`: is the pure matrix route (`xᵀ H x = (Uᵀ x)ᵀ D (Uᵀ x)`, `(Uᵀ x)_i = ⟨u_i, x⟩` via `mulVec_transpose`/`dotProduct`) the right one, or should I build an `OrthonormalBasis` from the columns? Any Mathlib lemma names you are confident about (e.g. `Matrix.dotProduct_mulVec`, `Matrix.vecMul_transpose`, `Matrix.mulVec_mulVec`, `Matrix.diagonal_mulVec`)?
3. For (B) at the Ω level (the chain `ulaChain` on a probability space) the expectation of `∑_i λ_i ⟨u_i, x_k⟩²` is `∑_i λ_i ∫ ⟨u_i, x_k⟩²` by linearity; the integrability of each `⟨u_i, x_k⟩²` comes from `MemLp 2`. Is there a slicker route through the measure-level `llc_ula_trajectory` (`t E_k[½⟨x, Hx⟩] = ½ tr(P(Σ - A^k Σ Aᵀ^k))`) plus its eigenbasis evaluation, avoiding the Ω-level chain? Which would you formalise?
4. Sizing: is A + B + C one tide (~400 lines)? Anything better close to this seabed that I missed (e.g. the first-order form of the inflation `≈ h t² tr(C)/4` with an explicit `O(h² p_max)` remainder, or the relative-error version)?
Vote for one candidate set at the end (`Vote: A+B+C`, `Vote: A+B`, …) and state any qualifications on the wording.
