You are consulted as GPT-6 Astra in a Lean 4 / Mathlib formalisation loop (the "Tide"). The attached tide log describes the seabed (a Lean repo `laplace`, Mathlib pinned Sep 2026) and three candidate targets A, B, C for one excursion. The seabed already has: `AR1Chain E rho v` (a chain `x : N -> E` in a real inner product space, `x 0 = 0`, `x (k+1) = rho • x k + eta (k+1)`, `inner (eta j) (eta k) = if j = k then v else 0`), with `x_eq_sum : x k = sum_{j<k} rho^j • eta (k-j)`, `inner_x_x : k <= l -> inner (x k) (x l) = v/(1-rho^2) * (rho^(l-k) - rho^(k+l))` (for rho^2 != 1), `inner_x_x_of_orthogonal` for two chains with orthogonal innovations; the L^2(Omega) realisation `realChain rho eta` with `toAR1Chain`, `expected_pooled_sample_variance` (moment-table hypothesis `hwhite`), `white_of_indep`; the ULA chain `ulaChain Q h xi = vecChain (euclid (1 - h•Q)) (sqrt(2h)) xi` with `inner_ulaChain_eq_realChain`, `projNoise`, `integral_projNoise_sq`, `integral_inner_sq_stdGaussian`, `expected_pooled_sample_variance_ula` (hypotheses: `xi c k` measurable, `P.map (xi c k) = stdGaussian (EuclideanSpace R iota)`, `iIndepFun (fun a : Fin C × N => xi a.1 a.2) P`); and from the previous tide `toeplitz_sum_le : sum_{i<N} sum_{j<N} rho^{dist i j} <= N * (1+rho)/(1-rho)` (for 0 <= rho < 1), `geom_sum_le_one_div`.

Questions:
1. Are A1–A4 correct as stated (in particular the sign/ordering `0 <= <x_k,x_l> <= sigma^2 rho^{|k-l|}` for a chain started at 0 with 0 <= rho < 1, and the inequality `(1+rho^2)/(1-rho^2) <= 1/(hp)` for rho = 1 - hp, 0 < hp <= 1)? Is the factor 2 in `Var(q sum f_a^2) = 2 q^2 sum_{a,b} E[f_a f_b]^2` right for a centred jointly Gaussian family?
2. A3: is the Finset induction the right Lean route for `E[X^2 Y^2] = |a|^2|b|^2 + 2<a,b>^2` with `X = sum a_i g_i`, `Y = sum b_i g_i`, `g_i` iid centred with moments (1, 0, 3)? Which Mathlib idioms give "g_n is independent of any measurable function of (g_i)_{i in s}" from `iIndepFun g P` (I believe `iIndepFun.indepFun_finset` + `IndepFun.comp`), and the integral factorisation `IndepFun.integral_mul_eq_mul_integral` / `IndepFun.integrable_mul`? Is it better to phrase the hypothesis as `MemLp (g i) 4 P` or as explicit integrability of the products?
3. A5: what is the current Mathlib route to the third and fourth moments of `<u, xi>` for `xi ~ stdGaussian (EuclideanSpace R iota)`: is there `IsGaussian`/`HasGaussianLaw` transport to `gaussianReal 0 |u|^2` and a moment lemma for `gaussianReal` (names?), or should I derive them from the coordinates being iid `gaussianReal 0 1` under `Measure.pi` plus the 1D moment integrals? If no clean route exists, is it acceptable to state A4 with the moment hypotheses on the projected innovations as explicit hypotheses and leave the discharge as a follow-up?
4. Is A (with B as a cheap companion) the right size for one tide? Anything better close to this seabed that I missed (e.g. a cleaner statement of the estimator law, such as the exact variance `2 sigma^4 sum_{k,l} (rho^{|k-l|} - rho^{k+l})^2 / N^2`)?
Vote for one candidate at the end (`Vote: A` etc.) and state any qualifications on the wording.
